/* AUTO-GENERATED from study-materials/leetcode-topics/*.md by build_topics.cjs.
   Edit the source there and rebuild: node build_topics.cjs */
const TOPICS = [
  {
    "slug": "recursion",
    "title": "Recursion — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Recursion isn't so much a pattern as the <strong>engine</strong> several patterns run on: <strong>DFS</strong>, <strong>backtracking</strong>, <strong>divide &amp; conquer</strong>, and <strong>top-down DP</strong> are all recursion with one extra bookkeeping habit bolted on. A function calls itself on a <strong>strictly smaller</strong> input until it hits a <strong>base case</strong> it can answer directly, then combines the results on the way back up.</p>\n<p><strong>Use when:</strong> the problem is <strong>self-similar</strong> — a tree/linked list, \"solve a smaller version of the same problem,\" nested structure, or a definition that refers to itself (factorial, <code>pow</code>, Fibonacci).</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-recursion.svg\" alt=\"Recursion call stack for factorial(4): descend to the base case, then combine back up\" loading=\"lazy\"></p>\n<p>Every correct recursion has exactly two parts:</p>\n<ol><li><strong>Base case(s)</strong> — the smallest inputs you answer <em>without</em> recursing. Miss one → infinite recursion → stack overflow.</li><li><strong>Recursive case</strong> — reduce to one or more <strong>strictly smaller</strong> subproblems, call yourself, and <strong>combine</strong> the results. \"Strictly smaller\" is what guarantees you reach a base case.</li></ol>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function solve(problem):\n    if is_base_case(problem):          # 1. stop condition — answer directly\n        return base_answer(problem)\n    subs = reduce(problem)             # 2. one or more strictly smaller subproblems\n    results = [solve(s) for s in subs]\n    return combine(results)            # 3. build this level's answer from the smaller ones</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>def factorial(n):\n    if n &lt;= 1:                 # base case\n        return 1\n    return n * factorial(n - 1)   # trust that factorial(n-1) is correct, then combine</code></pre>\n<p><strong>The leap of faith:</strong> don't trace the whole call tree in your head. Assume the recursive call <em>already returns the right answer for the smaller input</em>, verify your combine step and that the base case is reachable — induction does the rest.</p>\n<p><strong>Cost:</strong> time = (number of calls) × (work per call), usually captured as a <strong>recurrence</strong>; space = the <strong>maximum call-stack depth</strong> (each pending call holds a frame), so deep recursion is O(depth) memory even when each level does little. That depth is why Python caps recursion around 1000 and why very deep recursion is rewritten iteratively.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> the cleanest \"recurse on children, combine at the parent\" problem, <strong>Diameter of Binary Tree</strong>, runs in the editor → <a href=\"practice.html?p=diameter-of-binary-tree\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics with full Python/C++ solutions.</blockquote>\n<hr>\n<h2 id=\"1-pow-x-n\">1. Pow(x, n)</h2>\n<p>Compute <code>x^n</code>. Naive multiplication is O(n); <strong>halve the exponent</strong> instead — <code>x^n = (x^{n/2})^2</code> (times an extra <code>x</code> when <code>n</code> is odd) — for <strong>O(log n)</strong>. Handle negative <code>n</code> by inverting <code>x</code> and negating <code>n</code>. The single recursive call on <code>n/2</code> is what makes the recursion tree a <em>chain</em> of depth <code>log n</code>.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def my_pow(x: float, n: int) -&gt; float:\n    if n &lt; 0:\n        x, n = 1 / x, -n\n    def helper(k):\n        if k == 0:                 # base case: x^0 = 1\n            return 1.0\n        half = helper(k // 2)      # compute x^(k/2) ONCE\n        half_sq = half * half\n        return half_sq * x if k % 2 else half_sq   # odd k → one extra x\n    return helper(n)</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>double helper(double x, long long k) {\n    if (k == 0) return 1.0;              // base case\n    double half = helper(x, k / 2);      // compute x^(k/2) once\n    double sq = half * half;\n    return (k % 2) ? sq * x : sq;        // odd exponent → one extra x\n}\ndouble myPow(double x, int n) {\n    long long k = n;\n    if (k &lt; 0) { x = 1 / x; k = -k; }\n    return helper(x, k);\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-reverse-a-linked-list-recursively\">2. Reverse a Linked List (recursively)</h2>\n<p>Textbook <strong>linear recursion</strong>: recurse to the tail, then rewire pointers on the way back up. Trust that <code>reverse(head.next)</code> hands you the reversed rest; you only have to fix the single link between <code>head</code> and <code>head.next</code>. <strong>O(n)</strong> time, <strong>O(n)</strong> stack.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class ListNode:\n    def __init__(self, val=0, next=None):\n        self.val, self.next = val, next\n\ndef reverse_list(head: ListNode) -&gt; ListNode:\n    if head is None or head.next is None:   # base case: empty or single node\n        return head\n    new_head = reverse_list(head.next)      # reverse the rest (leap of faith)\n    head.next.next = head                   # make the next node point back at us\n    head.next = None                        # and cut our old forward link\n    return new_head                         # new head bubbles up unchanged</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>struct ListNode { int val; ListNode* next; };\n\nListNode* reverseList(ListNode* head) {\n    if (!head || !head-&gt;next) return head;      // base case\n    ListNode* newHead = reverseList(head-&gt;next); // reverse the rest\n    head-&gt;next-&gt;next = head;                     // point next node back at us\n    head-&gt;next = nullptr;                        // cut old forward link\n    return newHead;                              // new head is unchanged going up\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-merge-two-sorted-lists-recursively\">3. Merge Two Sorted Lists (recursively)</h2>\n<p>Recursion that <strong>builds structure</strong>: the smaller head is the answer's head; its <code>next</code> is \"the merge of the rest,\" which is the same problem on shorter input. Base case is when either list is empty. <strong>O(n + m)</strong> time, O(n + m) stack.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def merge_two_lists(a: ListNode, b: ListNode) -&gt; ListNode:\n    if a is None: return b        # base cases: one list exhausted\n    if b is None: return a\n    if a.val &lt;= b.val:\n        a.next = merge_two_lists(a.next, b)   # a leads; merge the rest behind it\n        return a\n    else:\n        b.next = merge_two_lists(a, b.next)\n        return b</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>ListNode* mergeTwoLists(ListNode* a, ListNode* b) {\n    if (!a) return b;                       // base cases\n    if (!b) return a;\n    if (a-&gt;val &lt;= b-&gt;val) {\n        a-&gt;next = mergeTwoLists(a-&gt;next, b);\n        return a;\n    } else {\n        b-&gt;next = mergeTwoLists(a, b-&gt;next);\n        return b;\n    }\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-fibonacci-naive-recursion-vs-memoized\">4. Fibonacci — naive recursion vs. memoized</h2>\n<p>The canonical lesson in <em>why recursion can be slow and how to fix it without changing the logic</em>. Naive <code>fib(n) = fib(n-1) + fib(n-2)</code> is <strong>branching</strong> recursion: the same arguments recur, so the tree has O(φⁿ) nodes — exponential. Because subproblems <strong>overlap</strong>, caching each result (<strong>memoization</strong>) collapses it to <strong>O(n)</strong>. That one step <em>is</em> top-down DP.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code># Naive: O(φ^n) time — every fib(k) is recomputed many times\ndef fib_slow(n):\n    if n &lt; 2:\n        return n\n    return fib_slow(n - 1) + fib_slow(n - 2)\n\n# Memoized: O(n) time, O(n) space — each fib(k) computed once\nfrom functools import lru_cache\n\n@lru_cache(maxsize=None)\ndef fib(n):\n    if n &lt; 2:\n        return n\n    return fib(n - 1) + fib(n - 2)</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\n// Memoized: each fib(k) computed once → O(n)\nint fibHelper(int n, std::vector&lt;int&gt;&amp; memo) {\n    if (n &lt; 2) return n;                 // base cases\n    if (memo[n] != -1) return memo[n];   // already solved this subproblem\n    return memo[n] = fibHelper(n - 1, memo) + fibHelper(n - 2, memo);\n}\nint fib(int n) {\n    std::vector&lt;int&gt; memo(n + 1, -1);\n    return fibHelper(n, memo);\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>What two parts must every recursion have, and what breaks if either is wrong?</summary><div class=\"qa-body\"><p>A <strong>base case</strong> (smallest input answered without recursing) and a <strong>recursive case</strong> that reduces to a <strong>strictly smaller</strong> subproblem and combines the results. A missing/wrong base case, or a recursive case that doesn't actually shrink the input, means the calls never stop → <strong>stack overflow</strong>. So state the base case <em>first</em>, then show each recursive call strictly moves toward it. For correctness, use the <strong>recursive leap of faith</strong>: assume the recursive call is right for the smaller input, then just check the combine step and termination.</p></div></details>\n<details class=\"qa\"><summary>How do you analyze the time and space complexity of a recursion?</summary><div class=\"qa-body\"><p><strong>Time</strong> = (number of calls) × (work per call), usually written as a <strong>recurrence</strong> and solved: <code>T(n)=T(n-1)+O(1)</code> → O(n) (factorial); <code>T(n)=2T(n/2)+O(n)</code> → O(n log n) by the <strong>Master Theorem</strong> (merge sort); <code>T(n)=T(n-1)+T(n-2)</code> → O(φⁿ) (naive Fibonacci). <strong>Space</strong> is the <strong>maximum call stack depth</strong> — O(n) for linear recursion, O(log n) for balanced divide &amp; conquer, O(height) for a tree walk. Note time and space can differ sharply: naive Fibonacci is exponential time but only O(n) stack.</p></div></details>\n<details class=\"qa\"><summary>How would you convert a recursive solution to iterative, and why?</summary><div class=\"qa-body\"><p>It depends on the shape. <strong>Tail / linear</strong> recursion → a plain <strong>loop</strong> with an accumulator. <strong>Branching</strong> recursion (tree/graph walks) → a loop with an <strong>explicit stack</strong> you push/pop yourself (that <em>is</em> iterative DFS). <strong>Overlapping-subproblem</strong> recursion → <strong>memoization</strong> or a <strong>bottom-up table</strong> (that <em>is</em> DP). Reasons to bother: avoid <strong>stack overflow</strong> on deep inputs, cut the constant-factor overhead of call frames, and sometimes reduce space. The logic is identical — you're just managing the stack explicitly instead of letting the language do it.</p></div></details>\n<details class=\"qa\"><summary>Recursion sits inside several patterns — how do they differ?</summary><div class=\"qa-body\"><p>Same engine, one extra habit each: <strong>DFS</strong> = recurse over neighbors, mark visited so you don't loop. <strong>Backtracking</strong> = recurse over choices but <strong>undo</strong> each after the call (choose → explore → un-choose). <strong>Divide &amp; conquer</strong> = split into <strong>independent</strong> halves, recurse, and <strong>combine</strong> (merge sort, quickselect). <strong>Top-down DP</strong> = recursion whose subproblems <strong>overlap</strong>, so you <strong>memoize</strong>. Litmus test on the recursion tree: independent branches → divide &amp; conquer; repeated arguments → DP; try/undo each option → backtracking; explore everything reachable → DFS.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Pow(x, n)",
        "id": "1-pow-x-n"
      },
      {
        "level": 2,
        "txt": "2. Reverse a Linked List (recursively)",
        "id": "2-reverse-a-linked-list-recursively"
      },
      {
        "level": 2,
        "txt": "3. Merge Two Sorted Lists (recursively)",
        "id": "3-merge-two-sorted-lists-recursively"
      },
      {
        "level": 2,
        "txt": "4. Fibonacci — naive recursion vs. memoized",
        "id": "4-fibonacci-naive-recursion-vs-memoized"
      }
    ]
  },
  {
    "slug": "two-pointers",
    "title": "Two Pointers — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Two indices walking an array — <strong>toward each other</strong> (pair problems on <strong>sorted</strong> data) or in the <strong>same direction</strong> (fast/slow, in-place writes). Turns many <code>O(n²)</code> scans into <code>O(n)</code> with <code>O(1)</code> extra space.</p>\n<p><strong>Use when:</strong> sorted array, pair/triple sums, palindrome checks, removing duplicates, partitioning, or merging two sorted sequences.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-two-pointers.svg\" alt=\"Two pointers converging on a sorted array\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function twoPointers(a):              # a is sorted\n    i ← 0;  j ← length(a) − 1\n    while i &lt; j:\n        if condition(a[i], a[j]) holds:\n            record / return (i, j)\n            i ← i + 1;  j ← j − 1      # move both inward\n        else if need a larger value:\n            i ← i + 1                 # advance the small end\n        else:                          # need a smaller value\n            j ← j − 1                 # retreat the big end</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code># Pair that sums to target in a SORTED array — O(n) time, O(1) space\ndef two_sum_sorted(a, target):\n    i, j = 0, len(a) - 1\n    while i &lt; j:\n        s = a[i] + a[j]\n        if s == target:   return (i, j)\n        elif s &lt; target:  i += 1      # need bigger → move left pointer up\n        else:             j -= 1      # need smaller → move right pointer down\n    return None</code></pre>\n<p>The <strong>fast/slow</strong> variant (in-place compaction, cycle detection in a linked list) keeps a <code>write</code> pointer behind a <code>read</code> pointer. Cycle detection (Floyd's) advances slow by 1, fast by 2; they meet iff there's a cycle.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> the canonical two-pointer problem, <strong>3Sum</strong>, is solvable in the editor → <a href=\"practice.html?p=3sum\" target=\"_blank\" rel=\"noopener\">open it</a>. The questions below are <em>additional</em> high-frequency ones with full Python/C++ solutions.</blockquote>\n<hr>\n<h2 id=\"1-valid-palindrome\">1. Valid Palindrome</h2>\n<p>Two pointers from both ends walk inward, skipping non-alphanumeric characters and comparing case-insensitively. <strong>O(n)</strong> time, <strong>O(1)</strong> space.</p>\n<p><strong>Key idea:</strong> don't build a cleaned copy of the string — filter on the fly so space stays constant.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def is_palindrome(s: str) -&gt; bool:\n    i, j = 0, len(s) - 1\n    while i &lt; j:\n        while i &lt; j and not s[i].isalnum(): i += 1   # skip junk from the left\n        while i &lt; j and not s[j].isalnum(): j -= 1   # skip junk from the right\n        if s[i].lower() != s[j].lower():\n            return False\n        i += 1; j -= 1\n    return True</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;cctype&gt;\n#include &lt;string&gt;\n\nbool isPalindrome(const std::string&amp; s) {\n    int i = 0, j = (int)s.size() - 1;\n    while (i &lt; j) {\n        while (i &lt; j &amp;&amp; !std::isalnum((unsigned char)s[i])) i++;\n        while (i &lt; j &amp;&amp; !std::isalnum((unsigned char)s[j])) j--;\n        if (std::tolower((unsigned char)s[i]) != std::tolower((unsigned char)s[j]))\n            return false;\n        i++; j--;\n    }\n    return true;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-container-with-most-water\">2. Container With Most Water</h2>\n<p>Height array; pick two lines forming a container with the most water. Start pointers at both ends; the area is <code>min(h[i], h[j]) * (j - i)</code>. <strong>Always move the shorter wall inward</strong> — the taller one can only ever be limited by a shorter partner, so moving it can't improve the area. <strong>O(n)</strong> time, <strong>O(1)</strong> space.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def max_area(height: list[int]) -&gt; int:\n    i, j, best = 0, len(height) - 1, 0\n    while i &lt; j:\n        best = max(best, min(height[i], height[j]) * (j - i))\n        if height[i] &lt; height[j]:   # move the limiting (shorter) wall\n            i += 1\n        else:\n            j -= 1\n    return best</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint maxArea(std::vector&lt;int&gt;&amp; height) {\n    int i = 0, j = (int)height.size() - 1, best = 0;\n    while (i &lt; j) {\n        best = std::max(best, std::min(height[i], height[j]) * (j - i));\n        if (height[i] &lt; height[j]) i++;   // move the shorter wall\n        else j--;\n    }\n    return best;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-two-sum-ii-input-array-is-sorted\">3. Two Sum II — Input Array Is Sorted</h2>\n<p>Return the 1-indexed pair that sums to <code>target</code>. Because the array is <strong>sorted</strong>, converging pointers give <code>O(n)</code>/<code>O(1)</code> — no hash map needed. If the sum is too small, advance the left pointer (need a bigger value); too big, retreat the right.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def two_sum(numbers: list[int], target: int) -&gt; list[int]:\n    i, j = 0, len(numbers) - 1\n    while i &lt; j:\n        s = numbers[i] + numbers[j]\n        if s == target:   return [i + 1, j + 1]   # problem uses 1-based indices\n        elif s &lt; target:  i += 1\n        else:             j -= 1\n    return []</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nstd::vector&lt;int&gt; twoSum(std::vector&lt;int&gt;&amp; numbers, int target) {\n    int i = 0, j = (int)numbers.size() - 1;\n    while (i &lt; j) {\n        int s = numbers[i] + numbers[j];\n        if (s == target) return {i + 1, j + 1};\n        else if (s &lt; target) i++;\n        else j--;\n    }\n    return {};\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-move-zeroes\">4. Move Zeroes</h2>\n<p>Move all zeros to the end in place, keeping the relative order of non-zeros. A <strong>fast/slow</strong> pair: <code>write</code> marks where the next non-zero goes; <code>read</code> scans. Swap on each non-zero. <strong>O(n)</strong> time, <strong>O(1)</strong> space.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def move_zeroes(nums: list[int]) -&gt; None:\n    write = 0\n    for read in range(len(nums)):\n        if nums[read] != 0:\n            nums[write], nums[read] = nums[read], nums[write]\n            write += 1</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;utility&gt;\n\nvoid moveZeroes(std::vector&lt;int&gt;&amp; nums) {\n    int write = 0;\n    for (int read = 0; read &lt; (int)nums.size(); read++) {\n        if (nums[read] != 0)\n            std::swap(nums[write++], nums[read]);\n    }\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Why do you move the <em>shorter</em> wall in Container With Most Water?</summary><div class=\"qa-body\"><p>The area is bounded by the <strong>shorter</strong> of the two walls times the width. If you moved the taller wall inward, the width shrinks and the height is still capped by the (unchanged) shorter wall — the area can only stay the same or drop. Moving the shorter wall is the only move that gives a chance at a taller limiting wall, so it's the only move that can improve the answer. That's why the greedy two-pointer sweep is provably optimal in one pass.</p></div></details>\n<details class=\"qa\"><summary>When do two pointers beat a hash map?</summary><div class=\"qa-body\"><p>When the input is <strong>sorted</strong> (or cheap to sort) and you want <code>O(1)</code> extra space, or you need pairs/triples defined by an order relation (closest sum, 3Sum, container area). A hash map wins when the input is <strong>unsorted</strong>, you must return <strong>original indices</strong>, or membership/counting is the real question. Rule of thumb: sorted + space-tight → two pointers; unsorted + index-reporting → hash map.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Valid Palindrome",
        "id": "1-valid-palindrome"
      },
      {
        "level": 2,
        "txt": "2. Container With Most Water",
        "id": "2-container-with-most-water"
      },
      {
        "level": 2,
        "txt": "3. Two Sum II — Input Array Is Sorted",
        "id": "3-two-sum-ii-input-array-is-sorted"
      },
      {
        "level": 2,
        "txt": "4. Move Zeroes",
        "id": "4-move-zeroes"
      }
    ]
  },
  {
    "slug": "sliding-window",
    "title": "Sliding Window — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>A two-pointer specialization for <strong>contiguous</strong> subarrays/substrings. Grow the window from the right; when it violates a constraint, shrink from the left. Each element enters and leaves at most once → <strong>O(n)</strong>. Keep a little window state (a count, a frequency map, a running sum) so the validity check is <code>O(1)</code>.</p>\n<p><strong>Use when:</strong> \"longest/shortest/at-most-k subarray,\" moving averages over a stream, \"smallest window containing …\".</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-sliding-window.svg\" alt=\"Sliding window growing right and shrinking left\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function slidingWindow(seq):\n    left ← 0\n    for right in 0 … length(seq) − 1:\n        add seq[right] to the window\n        while window is invalid:          # constraint broken\n            remove seq[left] from window\n            left ← left + 1\n        update answer with (right − left + 1)\n    return answer</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code># Longest substring without repeating characters — O(n)\ndef longest_unique(s):\n    seen = {}            # char -&gt; last index\n    left = best = 0\n    for right, c in enumerate(s):\n        if c in seen and seen[c] &gt;= left:\n            left = seen[c] + 1        # jump left past the previous occurrence\n        seen[c] = right\n        best = max(best, right - left + 1)\n    return best</code></pre>\n<p>For <strong>fixed-size</strong> windows (moving average, sliding-window max), add the entering element and pop the leaving one — a deque gives O(1) amortized max/min over the window.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> the canonical variable window, <strong>Longest Substring Without Repeating Characters</strong>, runs in the editor → <a href=\"practice.html?p=longest-substring-no-repeat\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional high-frequency windows.</blockquote>\n<hr>\n<h2 id=\"1-minimum-window-substring\">1. Minimum Window Substring</h2>\n<p>Smallest substring of <code>s</code> containing every character of <code>t</code> (with multiplicity). Expand right to satisfy the requirement, then contract left while it stays satisfied, recording the best. <code>have</code>/<code>need</code> counters track how many distinct chars are fully covered so the check is <code>O(1)</code>. <strong>O(|s| + |t|)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import Counter\n\ndef min_window(s: str, t: str) -&gt; str:\n    if not t or not s: return \"\"\n    need = Counter(t)\n    missing = len(t)                 # total chars still to cover (with multiplicity)\n    left = start = 0\n    best = (float(\"inf\"), 0)         # (length, left)\n    for right, c in enumerate(s):\n        if need[c] &gt; 0: missing -= 1\n        need[c] -= 1\n        while missing == 0:          # window covers t → try to shrink\n            if right - left + 1 &lt; best[0]:\n                best = (right - left + 1, left)\n            need[s[left]] += 1\n            if need[s[left]] &gt; 0: missing += 1\n            left += 1\n    return \"\" if best[0] == float(\"inf\") else s[best[1]:best[1] + best[0]]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;string&gt;\n#include &lt;unordered_map&gt;\n#include &lt;climits&gt;\n\nstd::string minWindow(std::string s, std::string t) {\n    if (t.empty() || s.empty()) return \"\";\n    std::unordered_map&lt;char, int&gt; need;\n    for (char c : t) need[c]++;\n    int missing = (int)t.size(), left = 0, bestLen = INT_MAX, bestL = 0;\n    for (int right = 0; right &lt; (int)s.size(); right++) {\n        if (need[s[right]] &gt; 0) missing--;\n        need[s[right]]--;\n        while (missing == 0) {\n            if (right - left + 1 &lt; bestLen) { bestLen = right - left + 1; bestL = left; }\n            need[s[left]]++;\n            if (need[s[left]] &gt; 0) missing++;\n            left++;\n        }\n    }\n    return bestLen == INT_MAX ? \"\" : s.substr(bestL, bestLen);\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-longest-repeating-character-replacement\">2. Longest Repeating Character Replacement</h2>\n<p>Longest substring you can make all-identical by replacing at most <code>k</code> characters. A window is valid when <code>(window length − count of its most frequent char) ≤ k</code>. Track <code>maxFreq</code>; when the window turns invalid, slide left by one. <strong>O(n)</strong> time, <strong>O(26)</strong> space.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import defaultdict\n\ndef character_replacement(s: str, k: int) -&gt; int:\n    count = defaultdict(int)\n    left = max_freq = best = 0\n    for right, c in enumerate(s):\n        count[c] += 1\n        max_freq = max(max_freq, count[c])\n        if (right - left + 1) - max_freq &gt; k:   # too many chars to replace\n            count[s[left]] -= 1\n            left += 1\n        best = max(best, right - left + 1)\n    return best</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;string&gt;\n#include &lt;algorithm&gt;\n\nint characterReplacement(std::string s, int k) {\n    int count[26] = {0};\n    int left = 0, maxFreq = 0, best = 0;\n    for (int right = 0; right &lt; (int)s.size(); right++) {\n        maxFreq = std::max(maxFreq, ++count[s[right] - 'A']);\n        if ((right - left + 1) - maxFreq &gt; k)\n            count[s[left++] - 'A']--;\n        best = std::max(best, right - left + 1);\n    }\n    return best;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-permutation-in-string\">3. Permutation in String</h2>\n<p>Does <code>s2</code> contain a permutation of <code>s1</code> as a substring? Slide a <strong>fixed-size</strong> window of length <code>|s1|</code> over <code>s2</code> and compare frequency vectors. Maintain a <code>matches</code> counter over 26 letters so each step is <code>O(1)</code>. <strong>O(|s2|)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def check_inclusion(s1: str, s2: str) -&gt; bool:\n    if len(s1) &gt; len(s2): return False\n    need = [0] * 26\n    win = [0] * 26\n    for c in s1: need[ord(c) - 97] += 1\n    for i, c in enumerate(s2):\n        win[ord(c) - 97] += 1\n        if i &gt;= len(s1):                       # drop the char leaving the window\n            win[ord(s2[i - len(s1)]) - 97] -= 1\n        if win == need:\n            return True\n    return False</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;string&gt;\n#include &lt;array&gt;\n\nbool checkInclusion(std::string s1, std::string s2) {\n    if (s1.size() &gt; s2.size()) return false;\n    std::array&lt;int, 26&gt; need{}, win{};\n    for (char c : s1) need[c - 'a']++;\n    for (int i = 0; i &lt; (int)s2.size(); i++) {\n        win[s2[i] - 'a']++;\n        if (i &gt;= (int)s1.size()) win[s2[i - s1.size()] - 'a']--;\n        if (win == need) return true;\n    }\n    return false;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-max-consecutive-ones-iii\">4. Max Consecutive Ones III</h2>\n<p>Longest run of 1s after flipping at most <code>k</code> zeros. A window is valid while it contains <code>≤ k</code> zeros; grow right, and when the zero count exceeds <code>k</code>, move left past one zero. <strong>O(n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def longest_ones(nums: list[int], k: int) -&gt; int:\n    left = zeros = best = 0\n    for right, x in enumerate(nums):\n        if x == 0: zeros += 1\n        while zeros &gt; k:                # too many flips used → shrink\n            if nums[left] == 0: zeros -= 1\n            left += 1\n        best = max(best, right - left + 1)\n    return best</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint longestOnes(std::vector&lt;int&gt;&amp; nums, int k) {\n    int left = 0, zeros = 0, best = 0;\n    for (int right = 0; right &lt; (int)nums.size(); right++) {\n        if (nums[right] == 0) zeros++;\n        while (zeros &gt; k) {\n            if (nums[left] == 0) zeros--;\n            left++;\n        }\n        best = std::max(best, right - left + 1);\n    }\n    return best;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Fixed-size vs. variable-size sliding window — how do you tell them apart?</summary><div class=\"qa-body\"><p>If the problem fixes the window length (e.g. \"permutation of s1\", \"moving average of size k\"), use a <strong>fixed</strong> window: add the entering element and remove the leaving one each step. If the window length is the thing you're optimizing (\"longest/shortest subarray with property P\"), use a <strong>variable</strong> window: grow right unconditionally, and shrink left only while the constraint is violated (or while it stays satisfied, for \"smallest\" problems).</p></div></details>\n<details class=\"qa\"><summary>Why is the total work O(n) even with a nested while-loop?</summary><div class=\"qa-body\"><p>Because <code>left</code> only ever moves <strong>forward</strong>, never resets. Across the whole scan <code>right</code> advances <code>n</code> times and <code>left</code> advances at most <code>n</code> times, so the inner loop runs <code>O(n)</code> times <em>in total</em>, not per outer step. It's an amortized argument — each element enters the window once and leaves at most once.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Minimum Window Substring",
        "id": "1-minimum-window-substring"
      },
      {
        "level": 2,
        "txt": "2. Longest Repeating Character Replacement",
        "id": "2-longest-repeating-character-replacement"
      },
      {
        "level": 2,
        "txt": "3. Permutation in String",
        "id": "3-permutation-in-string"
      },
      {
        "level": 2,
        "txt": "4. Max Consecutive Ones III",
        "id": "4-max-consecutive-ones-iii"
      }
    ]
  },
  {
    "slug": "bfs",
    "title": "BFS — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Explore level by level with a <strong>queue</strong>. On an <strong>unweighted</strong> graph/grid, the first time you reach a node is via a <strong>shortest path</strong> (fewest edges). That's the whole reason BFS exists. Mark nodes visited <strong>on enqueue</strong>, never on dequeue, so each node enters the queue once.</p>\n<p><strong>Use when:</strong> shortest path / minimum number of steps on an unweighted graph, level-order traversal, \"spread from multiple sources\" (multi-source BFS), grid flood fill where you need distance.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-bfs-dfs.svg\" alt=\"BFS vs DFS visit order on the same tree\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function bfs(start):\n    queue ← [start];  seen ← {start};  dist ← 0\n    while queue not empty:\n        for each node in the current level:   # fixed-size sweep\n            pop node\n            if node is goal: return dist\n            for nb in neighbors(node):\n                if nb not in seen:\n                    seen.add(nb)              # mark on ENQUEUE, not dequeue\n                    queue.push(nb)\n        dist ← dist + 1\n    return -1                                 # unreachable</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\ndef bfs_shortest(grid, start, goal):\n    R, C = len(grid), len(grid[0])\n    q = deque([(start, 0)])             # (cell, distance)\n    seen = {start}\n    while q:\n        (r, c), d = q.popleft()\n        if (r, c) == goal: return d\n        for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n            nr, nc = r+dr, c+dc\n            if 0 &lt;= nr &lt; R and 0 &lt;= nc &lt; C and grid[nr][nc] == 0 and (nr,nc) not in seen:\n                seen.add((nr, nc))      # mark on ENQUEUE, never on dequeue, to avoid dups\n                q.append(((nr, nc), d+1))\n    return -1</code></pre>\n<p>Weighted edges break BFS's shortest-path guarantee → use <strong>Dijkstra</strong> (a heap-ordered BFS). 0/1 weights → <strong>0-1 BFS</strong> with a deque.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> multi-source grid BFS, <strong>Rotting Oranges</strong>, runs in the editor → <a href=\"practice.html?p=rotting-oranges\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics.</blockquote>\n<hr>\n<h2 id=\"leveled-bfs-when-and-how\">Leveled BFS — when and how</h2>\n<p><strong>Leveled</strong> (a.k.a. level-order) BFS processes the queue <strong>one full layer at a time</strong> instead of node by node. Reach for it whenever the answer depends on <em>distance/depth</em>, not just on whether a node was visited.</p>\n<p><strong>How to spot it</strong> — look for any of these tells:</p>\n<ol><li><strong>\"Level\" / \"depth\" is in the output shape.</strong> You must return or reason about nodes grouped by how far they are from the start — level-order traversal (<code>List[List[int]]</code>), right-side view, average of levels, zigzag.</li><li><strong>\"Minimum steps / fewest moves / shortest time\"</strong> on an <em>unweighted</em> graph or grid. Each sweep equals one unit of distance, so the level counter <em>is</em> the answer — Word Ladder, Open the Lock, 01 Matrix.</li><li><strong>Simultaneous / multi-source spread.</strong> \"Everything spreads one step at a time, how long until X\" — seed all sources at level 0 and count sweeps: Rotting Oranges, Walls and Gates.</li></ol>\n<p>If you only need \"is B reachable?\" or a plain count with no notion of distance, you don't need leveling — but it costs nothing extra, so when in doubt, level.</p>\n<p><strong>The one trick:</strong> snapshot <code>len(q)</code> <em>before</em> the inner loop and pop exactly that many nodes. Everything left in the queue after the sweep is the next level — the children you pushed during the sweep won't be touched until the next <code>while</code> iteration.</p>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\ndef leveled_bfs(start):\n    q = deque([start]); seen = {start}; level = 0\n    while q:\n        for _ in range(len(q)):        # freeze size = exactly this level's nodes\n            node = q.popleft()\n            # ... process node at depth `level` ...\n            for nb in neighbors(node):\n                if nb not in seen:\n                    seen.add(nb)        # mark on ENQUEUE\n                    q.append(nb)\n        level += 1                      # one full sweep done → advance a level\n    return level</code></pre>\n<p><strong>Two ways to track distance — pick one, don't mix:</strong></p>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Style</th><th>How</th><th>Best for</th></tr></thead><tbody><tr><td><strong>Level sweep</strong></td><td><code>for _ in range(len(q))</code>, bump counter after each sweep</td><td>when you need nodes <em>grouped</em> per level (level-order, right-side view, zigzag)</td></tr><tr><td><strong>Distance in the tuple</strong></td><td>store <code>(node, dist)</code> in the queue, no inner loop</td><td>when you just need the number, not the grouping (the <code>bfs_shortest</code> template above)</td></tr></tbody></table></div>\n<p>Both give correct shortest distances. Use the sweep whenever a whole layer must act at once — e.g. <strong>Rotting Oranges</strong>, where each sweep = one minute and a full ring of oranges rots together:</p>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>def oranges_rotting(grid: list[list[int]]) -&gt; int:\n    R, C = len(grid), len(grid[0])\n    q, fresh = deque(), 0\n    for r in range(R):\n        for c in range(C):\n            if grid[r][c] == 2: q.append((r, c))    # all sources, level 0\n            elif grid[r][c] == 1: fresh += 1\n    minutes = 0\n    while q and fresh:                              # guard avoids a trailing empty minute\n        for _ in range(len(q)):                     # one minute = one sweep\n            r, c = q.popleft()\n            for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n                nr, nc = r + dr, c + dc\n                if 0 &lt;= nr &lt; R and 0 &lt;= nc &lt; C and grid[nr][nc] == 1:\n                    grid[nr][nc] = 2\n                    fresh -= 1\n                    q.append((nr, nc))\n        minutes += 1                                # advance only after a full ring rots\n    return -1 if fresh else minutes</code></pre>\n<hr>\n<h2 id=\"1-binary-tree-level-order-traversal\">1. Binary Tree Level Order Traversal</h2>\n<p>Return node values grouped by level. Process the queue one <strong>level at a time</strong>: record the current queue size, pop exactly that many nodes, and enqueue their children for the next level. <strong>O(n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val, self.left, self.right = val, left, right\n\ndef level_order(root: TreeNode) -&gt; list[list[int]]:\n    if not root: return []\n    res, q = [], deque([root])\n    while q:\n        level = []\n        for _ in range(len(q)):        # fixed-size sweep = one level\n            node = q.popleft()\n            level.append(node.val)\n            if node.left:  q.append(node.left)\n            if node.right: q.append(node.right)\n        res.append(level)\n    return res</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;queue&gt;\n\nstruct TreeNode {\n    int val; TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nstd::vector&lt;std::vector&lt;int&gt;&gt; levelOrder(TreeNode* root) {\n    std::vector&lt;std::vector&lt;int&gt;&gt; res;\n    if (!root) return res;\n    std::queue&lt;TreeNode*&gt; q; q.push(root);\n    while (!q.empty()) {\n        int n = q.size();\n        std::vector&lt;int&gt; level;\n        for (int i = 0; i &lt; n; i++) {\n            TreeNode* node = q.front(); q.pop();\n            level.push_back(node-&gt;val);\n            if (node-&gt;left)  q.push(node-&gt;left);\n            if (node-&gt;right) q.push(node-&gt;right);\n        }\n        res.push_back(level);\n    }\n    return res;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-01-matrix\">2. 01 Matrix</h2>\n<p>For each cell, the distance to the nearest 0. <strong>Multi-source BFS</strong>: seed the queue with <em>all</em> zeros at distance 0, then relax outward — the first time a 1-cell is reached is its shortest distance. <strong>O(R·C)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\ndef update_matrix(mat: list[list[int]]) -&gt; list[list[int]]:\n    R, C = len(mat), len(mat[0])\n    dist = [[-1] * C for _ in range(R)]\n    q = deque()\n    for r in range(R):\n        for c in range(C):\n            if mat[r][c] == 0:\n                dist[r][c] = 0\n                q.append((r, c))               # all sources start together\n    while q:\n        r, c = q.popleft()\n        for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n            nr, nc = r + dr, c + dc\n            if 0 &lt;= nr &lt; R and 0 &lt;= nc &lt; C and dist[nr][nc] == -1:\n                dist[nr][nc] = dist[r][c] + 1\n                q.append((nr, nc))\n    return dist</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;queue&gt;\n\nstd::vector&lt;std::vector&lt;int&gt;&gt; updateMatrix(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; mat) {\n    int R = mat.size(), C = mat[0].size();\n    std::vector&lt;std::vector&lt;int&gt;&gt; dist(R, std::vector&lt;int&gt;(C, -1));\n    std::queue&lt;std::pair&lt;int,int&gt;&gt; q;\n    for (int r = 0; r &lt; R; r++)\n        for (int c = 0; c &lt; C; c++)\n            if (mat[r][c] == 0) { dist[r][c] = 0; q.push({r, c}); }\n    int dirs[4][2] = {{1,0},{-1,0},{0,1},{0,-1}};\n    while (!q.empty()) {\n        auto [r, c] = q.front(); q.pop();\n        for (auto&amp; d : dirs) {\n            int nr = r + d[0], nc = c + d[1];\n            if (nr &gt;= 0 &amp;&amp; nr &lt; R &amp;&amp; nc &gt;= 0 &amp;&amp; nc &lt; C &amp;&amp; dist[nr][nc] == -1) {\n                dist[nr][nc] = dist[r][c] + 1;\n                q.push({nr, nc});\n            }\n        }\n    }\n    return dist;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-open-the-lock\">3. Open the Lock</h2>\n<p>Fewest moves to turn a 4-wheel lock from <code>\"0000\"</code> to <code>target</code>, avoiding deadends. State = the 4-digit string; each state has 8 neighbors (each wheel ±1). Plain BFS over the state graph gives the minimum number of moves. <strong>O(10⁴ · 8)</strong> states.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\ndef open_lock(deadends: list[str], target: str) -&gt; int:\n    dead = set(deadends)\n    if \"0000\" in dead: return -1\n    q = deque([(\"0000\", 0)])\n    seen = {\"0000\"}\n    while q:\n        state, steps = q.popleft()\n        if state == target: return steps\n        for i in range(4):\n            d = int(state[i])\n            for nd in ((d + 1) % 10, (d - 1) % 10):\n                nxt = state[:i] + str(nd) + state[i + 1:]\n                if nxt not in seen and nxt not in dead:\n                    seen.add(nxt)\n                    q.append((nxt, steps + 1))\n    return -1</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;string&gt;\n#include &lt;vector&gt;\n#include &lt;queue&gt;\n#include &lt;unordered_set&gt;\n\nint openLock(std::vector&lt;std::string&gt;&amp; deadends, std::string target) {\n    std::unordered_set&lt;std::string&gt; dead(deadends.begin(), deadends.end());\n    if (dead.count(\"0000\")) return -1;\n    std::queue&lt;std::pair&lt;std::string,int&gt;&gt; q; q.push({\"0000\", 0});\n    std::unordered_set&lt;std::string&gt; seen{\"0000\"};\n    while (!q.empty()) {\n        auto [state, steps] = q.front(); q.pop();\n        if (state == target) return steps;\n        for (int i = 0; i &lt; 4; i++) {\n            for (int delta : {1, 9}) {                 // +1 and -1 (mod 10)\n                std::string nxt = state;\n                nxt[i] = '0' + (state[i] - '0' + delta) % 10;\n                if (!seen.count(nxt) &amp;&amp; !dead.count(nxt)) {\n                    seen.insert(nxt);\n                    q.push({nxt, steps + 1});\n                }\n            }\n        }\n    }\n    return -1;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Why mark visited on enqueue rather than on dequeue?</summary><div class=\"qa-body\"><p>If you wait until a node is dequeued to mark it, the same node can be pushed by several neighbors before it's first processed, so it sits in the queue multiple times — that bloats memory and can break the <code>O(V+E)</code> bound. Marking it the instant it's enqueued guarantees each node is queued at most once. (Mark the start node before the loop, too.)</p></div></details>\n<details class=\"qa\"><summary>When does BFS stop giving shortest paths, and what replaces it?</summary><div class=\"qa-body\"><p>The moment edges carry <strong>different positive weights</strong> — BFS counts edges, not cost, so it can settle a node before a cheaper multi-edge path arrives. Use <strong>Dijkstra</strong> (a heap-ordered BFS) for non-negative weights, or <strong>0-1 BFS</strong> with a deque when weights are only 0 or 1. For graphs with negative edges, Bellman-Ford.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "Leveled BFS — when and how",
        "id": "leveled-bfs-when-and-how"
      },
      {
        "level": 2,
        "txt": "1. Binary Tree Level Order Traversal",
        "id": "1-binary-tree-level-order-traversal"
      },
      {
        "level": 2,
        "txt": "2. 01 Matrix",
        "id": "2-01-matrix"
      },
      {
        "level": 2,
        "txt": "3. Open the Lock",
        "id": "3-open-the-lock"
      }
    ]
  },
  {
    "slug": "dfs",
    "title": "DFS — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Go as deep as possible, then backtrack — naturally <strong>recursive</strong> (or an explicit stack). DFS doesn't find shortest paths, but it's the tool for <strong>reachability, connected components, cycle detection, topological sort, and enumerating all paths</strong>. On a grid it's the flood-fill workhorse.</p>\n<p><strong>Use when:</strong> counting/flooding regions, cloning or fully traversing a graph, \"can you reach X\", topological ordering, or any \"explore everything from here\" task where distance doesn't matter.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-bfs-dfs.svg\" alt=\"BFS vs DFS visit order on the same tree\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function dfs(node):\n    if node is invalid or already visited: return\n    mark node visited\n    ... pre-order work (e.g. count, record path) ...\n    for nb in neighbors(node):\n        dfs(nb)\n    ... post-order work ...               # topological order = post-order reversed</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code># Count connected components / islands in a grid — O(R*C)\ndef num_islands(grid):\n    R, C = len(grid), len(grid[0])\n    seen = set()\n    def dfs(r, c):\n        if not (0 &lt;= r &lt; R and 0 &lt;= c &lt; C) or grid[r][c] != '1' or (r,c) in seen:\n            return\n        seen.add((r, c))\n        for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n            dfs(r+dr, c+dc)\n    count = 0\n    for r in range(R):\n        for c in range(C):\n            if grid[r][c] == '1' and (r,c) not in seen:\n                dfs(r, c); count += 1\n    return count</code></pre>\n<p><strong>Topological sort</strong> (course scheduling, build/dependency order) is DFS on a DAG, pushing each node <em>after</em> its descendants (post-order) and reversing — or Kahn's algorithm (BFS on in-degrees). A back-edge during DFS = a <strong>cycle</strong> (so the schedule is impossible).</p>\n<blockquote class=\"lec-callout\"><strong>BFS vs. DFS in one line:</strong> need the <em>shortest</em> / <em>fewest steps</em> → BFS. Need <em>existence / all of them / ordering</em> → DFS. Both are O(V+E); BFS uses O(width) memory, DFS uses O(depth) — go iterative for very deep grids to dodge recursion limits.</blockquote>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> DFS topological sort, <strong>Course Schedule II</strong>, runs in the editor → <a href=\"practice.html?p=course-schedule-ii\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics (Number of Islands and Word Ladder are drilled in-site in other weeks, so they're not repeated here).</blockquote>\n<hr>\n<h2 id=\"1-clone-graph\">1. Clone Graph</h2>\n<p>Deep-copy a connected undirected graph. DFS from the start node, keeping a <code>old → new</code> map so each node is cloned exactly once; the map also breaks cycles (return the existing clone on a revisit). <strong>O(V+E)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class Node:\n    def __init__(self, val=0, neighbors=None):\n        self.val = val\n        self.neighbors = neighbors or []\n\ndef clone_graph(node: \"Node\") -&gt; \"Node\":\n    if not node: return None\n    clones = {}\n    def dfs(cur):\n        if cur in clones:\n            return clones[cur]              # already cloned → reuse (handles cycles)\n        copy = Node(cur.val)\n        clones[cur] = copy                  # record BEFORE recursing\n        for nb in cur.neighbors:\n            copy.neighbors.append(dfs(nb))\n        return copy\n    return dfs(node)</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;unordered_map&gt;\n#include &lt;vector&gt;\n#include &lt;functional&gt;\n\nclass Node {\npublic:\n    int val;\n    std::vector&lt;Node*&gt; neighbors;\n    Node(int v) : val(v) {}\n};\n\nNode* cloneGraph(Node* node) {\n    if (!node) return nullptr;\n    std::unordered_map&lt;Node*, Node*&gt; clones;\n    std::function&lt;Node*(Node*)&gt; dfs = [&amp;](Node* cur) -&gt; Node* {\n        auto it = clones.find(cur);\n        if (it != clones.end()) return it-&gt;second;\n        Node* copy = new Node(cur-&gt;val);\n        clones[cur] = copy;                 // record before recursing\n        for (Node* nb : cur-&gt;neighbors)\n            copy-&gt;neighbors.push_back(dfs(nb));\n        return copy;\n    };\n    return dfs(node);\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-max-area-of-island\">2. Max Area of Island</h2>\n<p>Largest connected region of 1s (4-directionally) in a grid. DFS from each unvisited land cell, sinking cells as you count them so nothing is double-counted. <strong>O(R·C)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def max_area_of_island(grid: list[list[int]]) -&gt; int:\n    R, C = len(grid), len(grid[0])\n    def dfs(r, c):\n        if not (0 &lt;= r &lt; R and 0 &lt;= c &lt; C) or grid[r][c] == 0:\n            return 0\n        grid[r][c] = 0                        # sink to avoid revisiting\n        return 1 + dfs(r+1,c) + dfs(r-1,c) + dfs(r,c+1) + dfs(r,c-1)\n    best = 0\n    for r in range(R):\n        for c in range(C):\n            if grid[r][c] == 1:\n                best = max(best, dfs(r, c))\n    return best</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint dfs(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; g, int r, int c) {\n    int R = g.size(), C = g[0].size();\n    if (r &lt; 0 || r &gt;= R || c &lt; 0 || c &gt;= C || g[r][c] == 0) return 0;\n    g[r][c] = 0;\n    return 1 + dfs(g,r+1,c) + dfs(g,r-1,c) + dfs(g,r,c+1) + dfs(g,r,c-1);\n}\nint maxAreaOfIsland(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; grid) {\n    int best = 0;\n    for (int r = 0; r &lt; (int)grid.size(); r++)\n        for (int c = 0; c &lt; (int)grid[0].size(); c++)\n            if (grid[r][c] == 1) best = std::max(best, dfs(grid, r, c));\n    return best;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-pacific-atlantic-water-flow\">3. Pacific Atlantic Water Flow</h2>\n<p>Cells from which water can reach <strong>both</strong> oceans. Instead of searching from every cell, DFS <strong>inward from the borders</strong>: mark all cells reachable from the Pacific edge and, separately, from the Atlantic edge, flowing to <strong>higher-or-equal</strong> neighbors. The answer is the intersection. <strong>O(R·C)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def pacific_atlantic(heights: list[list[int]]) -&gt; list[list[int]]:\n    R, C = len(heights), len(heights[0])\n    pac, atl = set(), set()\n    def dfs(r, c, seen, prev):\n        if (r, c) in seen or not (0 &lt;= r &lt; R and 0 &lt;= c &lt; C) or heights[r][c] &lt; prev:\n            return\n        seen.add((r, c))\n        for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n            dfs(r+dr, c+dc, seen, heights[r][c])\n    for r in range(R):\n        dfs(r, 0, pac, 0); dfs(r, C-1, atl, 0)\n    for c in range(C):\n        dfs(0, c, pac, 0); dfs(R-1, c, atl, 0)\n    return [[r, c] for r, c in pac &amp; atl]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nvoid dfs(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; h, std::vector&lt;std::vector&lt;bool&gt;&gt;&amp; seen,\n         int r, int c, int prev) {\n    int R = h.size(), C = h[0].size();\n    if (r &lt; 0 || r &gt;= R || c &lt; 0 || c &gt;= C || seen[r][c] || h[r][c] &lt; prev) return;\n    seen[r][c] = true;\n    dfs(h, seen, r+1, c, h[r][c]); dfs(h, seen, r-1, c, h[r][c]);\n    dfs(h, seen, r, c+1, h[r][c]); dfs(h, seen, r, c-1, h[r][c]);\n}\nstd::vector&lt;std::vector&lt;int&gt;&gt; pacificAtlantic(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; heights) {\n    int R = heights.size(), C = heights[0].size();\n    std::vector&lt;std::vector&lt;bool&gt;&gt; pac(R, std::vector&lt;bool&gt;(C)), atl = pac;\n    for (int r = 0; r &lt; R; r++) { dfs(heights, pac, r, 0, 0); dfs(heights, atl, r, C-1, 0); }\n    for (int c = 0; c &lt; C; c++) { dfs(heights, pac, 0, c, 0); dfs(heights, atl, R-1, c, 0); }\n    std::vector&lt;std::vector&lt;int&gt;&gt; res;\n    for (int r = 0; r &lt; R; r++)\n        for (int c = 0; c &lt; C; c++)\n            if (pac[r][c] &amp;&amp; atl[r][c]) res.push_back({r, c});\n    return res;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>BFS vs. DFS — one line each on when to use which.</summary><div class=\"qa-body\"><p>Need the <strong>shortest path / fewest steps</strong> on unweighted edges, or level-by-level output → BFS. Need <strong>existence, all of them, components, cycles, or an ordering</strong> → DFS. Both are <code>O(V+E)</code>; BFS costs <code>O(width)</code> memory, DFS <code>O(depth)</code> — so on very deep grids/graphs, go <strong>iterative</strong> with an explicit stack to dodge recursion-limit stack overflows.</p></div></details>\n<details class=\"qa\"><summary>In Clone Graph, why record the clone in the map <em>before</em> recursing into neighbors?</summary><div class=\"qa-body\"><p>Because the graph has <strong>cycles</strong>. If node A neighbors B and B neighbors A, cloning A recurses into B, which recurses back into A. If A's clone isn't already in the map at that point, you recurse forever. Inserting <code>clones[A] = copyA</code> before descending means the back-edge finds the existing clone and returns immediately.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Clone Graph",
        "id": "1-clone-graph"
      },
      {
        "level": 2,
        "txt": "2. Max Area of Island",
        "id": "2-max-area-of-island"
      },
      {
        "level": 2,
        "txt": "3. Pacific Atlantic Water Flow",
        "id": "3-pacific-atlantic-water-flow"
      }
    ]
  },
  {
    "slug": "binary-search",
    "title": "Binary Search — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Halve the search space each step → <strong>O(log n)</strong>. Beyond \"find x in a sorted array,\" the real power is <strong>binary search on the answer</strong>: if you can cheaply test \"is a candidate answer feasible?\" and feasibility is <strong>monotonic</strong>, binary-search the smallest/largest feasible value. Get the <code>[lo, hi)</code> invariant and the <code>&lt;</code> vs <code>&lt;=</code> right and off-by-ones disappear.</p>\n<p><strong>Use when:</strong> anything \"sorted\" + search, \"minimize the max / maximize the min\", \"smallest value such that …\", or a rotated / 2-D sorted structure.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-binary-search.svg\" alt=\"Binary search halving the range each step\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function binarySearch(lo, hi):            # a sorted index range, or an answer range\n    while lo &lt; hi:\n        mid ← lo + (hi − lo) / 2          # floor; avoids overflow\n        if feasible(mid):                 # predicate must be monotonic\n            hi ← mid                      # keep mid, search the left half\n        else:\n            lo ← mid + 1                  # discard mid and everything left\n    return lo                             # smallest feasible value</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code># Smallest index where a[i] &gt;= target (lower_bound). Get the half-open invariant right.\ndef lower_bound(a, target):\n    lo, hi = 0, len(a)               # search range [lo, hi)\n    while lo &lt; hi:\n        mid = (lo + hi) // 2          # // is floor; no overflow worries in Python\n        if a[mid] &lt; target: lo = mid + 1\n        else:               hi = mid\n    return lo                         # in [0, len(a)]</code></pre>\n<p><strong>Search-on-answer pattern:</strong> \"minimum capacity to ship in D days,\" \"smallest divisor,\" \"split array largest sum.\" Write <code>feasible(x)</code>, then binary-search x. The monotonicity (<code>feasible</code> flips false→true exactly once) is what licenses it.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> <strong>Search in Rotated Sorted Array</strong> runs in the editor → <a href=\"practice.html?p=search-in-rotated-sorted-array\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics including the search-on-answer pattern.</blockquote>\n<hr>\n<h2 id=\"1-koko-eating-bananas\">1. Koko Eating Bananas</h2>\n<p>Smallest eating speed <code>k</code> so Koko finishes all piles within <code>h</code> hours. <strong>Search on the answer:</strong> <code>feasible(k)</code> = <code>sum(ceil(pile / k)) ≤ h</code>, which is monotonic in <code>k</code>. Binary-search the smallest feasible <code>k</code> in <code>[1, max(piles)]</code>. <strong>O(n log max)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import math\n\ndef min_eating_speed(piles: list[int], h: int) -&gt; int:\n    def hours(k):                     # hours needed at speed k\n        return sum(math.ceil(p / k) for p in piles)\n    lo, hi = 1, max(piles)\n    while lo &lt; hi:\n        mid = (lo + hi) // 2\n        if hours(mid) &lt;= h:           # feasible → try slower\n            hi = mid\n        else:\n            lo = mid + 1\n    return lo</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint minEatingSpeed(std::vector&lt;int&gt;&amp; piles, int h) {\n    auto hours = [&amp;](long long k) {\n        long long t = 0;\n        for (int p : piles) t += (p + k - 1) / k;   // ceil(p/k)\n        return t;\n    };\n    int lo = 1, hi = *std::max_element(piles.begin(), piles.end());\n    while (lo &lt; hi) {\n        int mid = lo + (hi - lo) / 2;\n        if (hours(mid) &lt;= h) hi = mid;\n        else lo = mid + 1;\n    }\n    return lo;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-find-minimum-in-rotated-sorted-array\">2. Find Minimum in Rotated Sorted Array</h2>\n<p>A sorted array rotated at an unknown pivot; find the minimum in <code>O(log n)</code>. Compare <code>mid</code> to the right end: if <code>nums[mid] &gt; nums[hi]</code> the minimum lies to the <strong>right</strong> of <code>mid</code>; otherwise it's at <code>mid</code> or to its left. Converges on the pivot.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def find_min(nums: list[int]) -&gt; int:\n    lo, hi = 0, len(nums) - 1\n    while lo &lt; hi:\n        mid = (lo + hi) // 2\n        if nums[mid] &gt; nums[hi]:      # min must be to the right of mid\n            lo = mid + 1\n        else:                          # min is mid or to its left\n            hi = mid\n    return nums[lo]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nint findMin(std::vector&lt;int&gt;&amp; nums) {\n    int lo = 0, hi = (int)nums.size() - 1;\n    while (lo &lt; hi) {\n        int mid = lo + (hi - lo) / 2;\n        if (nums[mid] &gt; nums[hi]) lo = mid + 1;\n        else hi = mid;\n    }\n    return nums[lo];\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-find-first-and-last-position-of-element-in-sorted-array\">3. Find First and Last Position of Element in Sorted Array</h2>\n<p>Return the start and end indices of <code>target</code>. Two binary searches: <code>lower_bound</code> (first index <code>≥ target</code>) and <code>upper_bound</code> (first index <code>&gt; target</code>). If the target is absent, return <code>[-1, -1]</code>. <strong>O(log n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from bisect import bisect_left, bisect_right\n\ndef search_range(nums: list[int], target: int) -&gt; list[int]:\n    lo = bisect_left(nums, target)\n    if lo == len(nums) or nums[lo] != target:\n        return [-1, -1]\n    hi = bisect_right(nums, target) - 1\n    return [lo, hi]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nstd::vector&lt;int&gt; searchRange(std::vector&lt;int&gt;&amp; nums, int target) {\n    auto lower = [&amp;](int t) {\n        int lo = 0, hi = nums.size();\n        while (lo &lt; hi) { int m = lo + (hi - lo) / 2; if (nums[m] &lt; t) lo = m + 1; else hi = m; }\n        return lo;\n    };\n    int lo = lower(target);\n    if (lo == (int)nums.size() || nums[lo] != target) return {-1, -1};\n    int hi = lower(target + 1) - 1;\n    return {lo, hi};\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-search-a-2d-matrix\">4. Search a 2D Matrix</h2>\n<p>Rows sorted, and each row's first element exceeds the previous row's last — so the matrix reads as one sorted list of length <code>m·n</code>. Binary-search that virtual array, mapping index <code>k</code> to <code>(k / n, k % n)</code>. <strong>O(log(m·n))</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def search_matrix(matrix: list[list[int]], target: int) -&gt; bool:\n    m, n = len(matrix), len(matrix[0])\n    lo, hi = 0, m * n - 1\n    while lo &lt;= hi:\n        mid = (lo + hi) // 2\n        val = matrix[mid // n][mid % n]     # treat as a flat sorted array\n        if val == target:  return True\n        elif val &lt; target: lo = mid + 1\n        else:              hi = mid - 1\n    return False</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nbool searchMatrix(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; matrix, int target) {\n    int m = matrix.size(), n = matrix[0].size();\n    int lo = 0, hi = m * n - 1;\n    while (lo &lt;= hi) {\n        int mid = lo + (hi - lo) / 2;\n        int val = matrix[mid / n][mid % n];\n        if (val == target) return true;\n        else if (val &lt; target) lo = mid + 1;\n        else hi = mid - 1;\n    }\n    return false;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>What makes \"binary search on the answer\" valid?</summary><div class=\"qa-body\"><p>A <strong>monotonic predicate</strong>. If <code>feasible(x)</code> is false for all small <code>x</code> and true for all large <code>x</code> (or vice-versa), there's a single false→true boundary, and binary search finds it. You never touch the input in sorted order — you search the <em>answer range</em> and call <code>feasible</code> at each midpoint. The whole trick is proving feasibility flips exactly once; without that, binary search is unsound.</p></div></details>\n<details class=\"qa\"><summary>Why prefer the half-open <code>[lo, hi)</code> invariant?</summary><div class=\"qa-body\"><p>It removes the two most common bugs: the <code>mid + 1</code> / <code>mid</code> update is uniform (<code>lo = mid + 1</code> when rejecting, <code>hi = mid</code> when keeping), the loop condition is simply <code>lo &lt; hi</code>, and the loop can return the boundary <code>lo</code> in <code>[0, n]</code> — including \"insert at the end\". Mixing <code>&lt;=</code> with <code>hi = mid</code> is where infinite loops and off-by-ones creep in.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Koko Eating Bananas",
        "id": "1-koko-eating-bananas"
      },
      {
        "level": 2,
        "txt": "2. Find Minimum in Rotated Sorted Array",
        "id": "2-find-minimum-in-rotated-sorted-array"
      },
      {
        "level": 2,
        "txt": "3. Find First and Last Position of Element in Sorted Array",
        "id": "3-find-first-and-last-position-of-element-in-sorted-array"
      },
      {
        "level": 2,
        "txt": "4. Search a 2D Matrix",
        "id": "4-search-a-2d-matrix"
      }
    ]
  },
  {
    "slug": "divide-and-conquer",
    "title": "Divide & Conquer — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Split into <strong>independent</strong> subproblems, solve recursively, then <strong>combine</strong> — the merge step does the real work. Cost follows the Master theorem <code>T(n) = a·T(n/b) + O(combine)</code>.</p>\n<p><strong>Use when:</strong> merge sort / quickselect, \"count while you merge\" (inversions), maximum subarray by halves, building balanced trees, closest pair of points.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-divide-conquer.svg\" alt=\"Merge sort: split down, merge up\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function solve(problem):\n    if problem is small: return baseCase(problem)\n    parts   ← split(problem)               # divide\n    results ← [ solve(p) for p in parts ]  # conquer\n    return combine(results)                # the merge step does the real work</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>def merge_sort(a):\n    if len(a) &lt;= 1: return a\n    mid = len(a) // 2\n    L, R = merge_sort(a[:mid]), merge_sort(a[mid:])   # divide + conquer\n    out, i, j = [], 0, 0                                # combine: merge two sorted halves\n    while i &lt; len(L) and j &lt; len(R):\n        if L[i] &lt;= R[j]: out.append(L[i]); i += 1\n        else:            out.append(R[j]); j += 1\n    out.extend(L[i:]); out.extend(R[j:])\n    return out                                          # O(n log n), stable</code></pre>\n<p><strong>Quickselect</strong> (kth-smallest in average O(n)) is divide-and-conquer that recurses into <em>only one</em> side. The line between D&amp;C and DP: D&amp;C subproblems <strong>don't overlap</strong>; when they <em>do</em>, you memoize and it becomes DP.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> <strong>Merge k Sorted Lists</strong> runs in the editor → <a href=\"practice.html?p=merge-k-sorted-lists\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics.</blockquote>\n<hr>\n<h2 id=\"1-sort-an-array-merge-sort\">1. Sort an Array (merge sort)</h2>\n<p>Sort in guaranteed <code>O(n log n)</code> with a stable merge sort: split in half, sort each half, merge. The merge walks two sorted halves with two pointers.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def sort_array(nums: list[int]) -&gt; list[int]:\n    if len(nums) &lt;= 1:\n        return nums\n    mid = len(nums) // 2\n    left = sort_array(nums[:mid])\n    right = sort_array(nums[mid:])\n    merged, i, j = [], 0, 0\n    while i &lt; len(left) and j &lt; len(right):\n        if left[i] &lt;= right[j]:            # &lt;= keeps it stable\n            merged.append(left[i]); i += 1\n        else:\n            merged.append(right[j]); j += 1\n    merged.extend(left[i:]); merged.extend(right[j:])\n    return merged</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nstd::vector&lt;int&gt; sortArray(std::vector&lt;int&gt;&amp; nums) {\n    if (nums.size() &lt;= 1) return nums;\n    int mid = nums.size() / 2;\n    std::vector&lt;int&gt; left(nums.begin(), nums.begin() + mid);\n    std::vector&lt;int&gt; right(nums.begin() + mid, nums.end());\n    left = sortArray(left); right = sortArray(right);\n    std::vector&lt;int&gt; merged;\n    int i = 0, j = 0;\n    while (i &lt; (int)left.size() &amp;&amp; j &lt; (int)right.size()) {\n        if (left[i] &lt;= right[j]) merged.push_back(left[i++]);\n        else merged.push_back(right[j++]);\n    }\n    while (i &lt; (int)left.size()) merged.push_back(left[i++]);\n    while (j &lt; (int)right.size()) merged.push_back(right[j++]);\n    return merged;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-kth-largest-element-in-an-array-quickselect\">2. Kth Largest Element in an Array (quickselect)</h2>\n<p>The average-<code>O(n)</code> cousin of quicksort: partition around a pivot, then recurse into <strong>only the side</strong> that contains the k-th largest. Worst case <code>O(n²)</code>, avoided in practice with a random pivot.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import random\n\ndef find_kth_largest(nums: list[int], k: int) -&gt; int:\n    target = len(nums) - k                 # k-th largest = target-th smallest (0-based)\n    lo, hi = 0, len(nums) - 1\n    while lo &lt;= hi:\n        p = partition(nums, lo, hi)\n        if   p == target: return nums[p]\n        elif p &lt; target:  lo = p + 1       # recurse right only\n        else:             hi = p - 1       # recurse left only\n    return -1\n\ndef partition(a, lo, hi):\n    i = random.randint(lo, hi)\n    a[i], a[hi] = a[hi], a[i]              # random pivot to the end\n    pivot, store = a[hi], lo\n    for j in range(lo, hi):\n        if a[j] &lt; pivot:\n            a[store], a[j] = a[j], a[store]; store += 1\n    a[store], a[hi] = a[hi], a[store]\n    return store</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;cstdlib&gt;\n#include &lt;utility&gt;\n\nint partition(std::vector&lt;int&gt;&amp; a, int lo, int hi) {\n    int i = lo + rand() % (hi - lo + 1);\n    std::swap(a[i], a[hi]);\n    int pivot = a[hi], store = lo;\n    for (int j = lo; j &lt; hi; j++)\n        if (a[j] &lt; pivot) std::swap(a[store++], a[j]);\n    std::swap(a[store], a[hi]);\n    return store;\n}\nint findKthLargest(std::vector&lt;int&gt;&amp; nums, int k) {\n    int target = (int)nums.size() - k, lo = 0, hi = nums.size() - 1;\n    while (lo &lt;= hi) {\n        int p = partition(nums, lo, hi);\n        if (p == target) return nums[p];\n        else if (p &lt; target) lo = p + 1;\n        else hi = p - 1;\n    }\n    return -1;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-count-of-smaller-numbers-after-self\">3. Count of Smaller Numbers After Self</h2>\n<p>For each element, how many elements to its right are smaller. Do it <strong>while merge-sorting</strong> indices: when the right half contributes an element before a left-half element, every element already merged from the right is smaller than that left element — accumulate those counts. <strong>O(n log n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def count_smaller(nums: list[int]) -&gt; list[int]:\n    counts = [0] * len(nums)\n    idx = list(range(len(nums)))            # sort indices, not values\n\n    def sort(lo, hi):\n        if hi - lo &lt;= 1: return\n        mid = (lo + hi) // 2\n        sort(lo, mid); sort(mid, hi)\n        merged, i, j, right_smaller = [], lo, mid, 0\n        while i &lt; mid and j &lt; hi:\n            if nums[idx[j]] &lt; nums[idx[i]]:\n                right_smaller += 1          # this right element is smaller\n                merged.append(idx[j]); j += 1\n            else:\n                counts[idx[i]] += right_smaller\n                merged.append(idx[i]); i += 1\n        while i &lt; mid:\n            counts[idx[i]] += right_smaller\n            merged.append(idx[i]); i += 1\n        while j &lt; hi:\n            merged.append(idx[j]); j += 1\n        idx[lo:hi] = merged\n\n    sort(0, len(nums))\n    return counts</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nvoid sortCount(std::vector&lt;int&gt;&amp; nums, std::vector&lt;int&gt;&amp; idx,\n               std::vector&lt;int&gt;&amp; counts, int lo, int hi) {\n    if (hi - lo &lt;= 1) return;\n    int mid = (lo + hi) / 2;\n    sortCount(nums, idx, counts, lo, mid);\n    sortCount(nums, idx, counts, mid, hi);\n    std::vector&lt;int&gt; merged;\n    int i = lo, j = mid, rightSmaller = 0;\n    while (i &lt; mid &amp;&amp; j &lt; hi) {\n        if (nums[idx[j]] &lt; nums[idx[i]]) { rightSmaller++; merged.push_back(idx[j++]); }\n        else { counts[idx[i]] += rightSmaller; merged.push_back(idx[i++]); }\n    }\n    while (i &lt; mid) { counts[idx[i]] += rightSmaller; merged.push_back(idx[i++]); }\n    while (j &lt; hi) merged.push_back(idx[j++]);\n    for (int k = 0; k &lt; (int)merged.size(); k++) idx[lo + k] = merged[k];\n}\nstd::vector&lt;int&gt; countSmaller(std::vector&lt;int&gt;&amp; nums) {\n    int n = nums.size();\n    std::vector&lt;int&gt; counts(n, 0), idx(n);\n    for (int i = 0; i &lt; n; i++) idx[i] = i;\n    sortCount(nums, idx, counts, 0, n);\n    return counts;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Divide-and-conquer vs. dynamic programming — the litmus test?</summary><div class=\"qa-body\"><p>Draw the recursion tree. If the same subproblem (same arguments) appears in <strong>multiple</strong> branches, the subproblems <strong>overlap</strong> → memoize/tabulate → it's DP. If each subproblem is solved <strong>once</strong> and the branches are independent (merge sort's two halves), it's plain divide and conquer — no cache needed.</p></div></details>\n<details class=\"qa\"><summary>Why does quickselect average O(n) while quicksort is O(n log n)?</summary><div class=\"qa-body\"><p>Quicksort recurses into <strong>both</strong> partitions; quickselect recurses into <strong>one</strong>. With a balanced split the work is <code>n + n/2 + n/4 + … = O(n)</code> (a geometric series), versus quicksort's <code>O(n)</code> per level across <code>log n</code> levels. A random (or median-of-medians) pivot keeps the split balanced in expectation, avoiding the <code>O(n²)</code> worst case.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Sort an Array (merge sort)",
        "id": "1-sort-an-array-merge-sort"
      },
      {
        "level": 2,
        "txt": "2. Kth Largest Element in an Array (quickselect)",
        "id": "2-kth-largest-element-in-an-array-quickselect"
      },
      {
        "level": 2,
        "txt": "3. Count of Smaller Numbers After Self",
        "id": "3-count-of-smaller-numbers-after-self"
      }
    ]
  },
  {
    "slug": "dynamic-programming",
    "title": "Dynamic Programming — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>When subproblems <strong>overlap</strong> and the problem has <strong>optimal substructure</strong> (the optimum is built from optima of subproblems), cache results. Two equivalent styles:</p>\n<ul><li><strong>Top-down (memoization):</strong> recursion + a cache. Easiest to derive — write the brute-force recurrence, then <code>@lru_cache</code> it.</li><li><strong>Bottom-up (tabulation):</strong> fill a table in dependency order. Lets you shrink memory (often to O(1) rows).</li></ul>\n<p><strong>Use when:</strong> \"max / min / count the ways / can you reach\", with choices that reuse overlapping sub-answers.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-dp-table.svg\" alt=\"Coin change DP table and the transition into the last cell\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template, bottom-up):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>define dp[state] = answer to the subproblem `state`\nset base cases\nfor state in dependency order:\n    dp[state] ← aggregate over choices:        # min / max / count / OR\n        combine( dp[smaller_state], cost(choice) )\nreturn dp[target]</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code># Coin change: fewest coins to make `amount` — classic 1-D DP, O(amount * #coins)\ndef coin_change(coins, amount):\n    INF = amount + 1\n    dp = [0] + [INF] * amount         # dp[x] = fewest coins to make x\n    for x in range(1, amount + 1):\n        for c in coins:\n            if c &lt;= x:\n                dp[x] = min(dp[x], dp[x - c] + 1)\n    return dp[amount] if dp[amount] != INF else -1</code></pre>\n<p><strong>How to derive any DP:</strong> (1) define the state (what does <code>dp[i]</code> <em>mean</em>?), (2) write the transition (how does it build from smaller states?), (3) set base cases, (4) decide the fill order. Common families: 1-D sequences (house robber, climbing stairs), <strong>Kadane's</strong> (max subarray — running best ending here), 2-D grids (edit distance, LCS, <strong>maximal square</strong>), knapsack (subset sum), and intervals (matrix-chain, burst balloons).</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> the canonical unbounded-knapsack DP, <strong>Coin Change</strong>, runs in the editor → <a href=\"practice.html?p=coin-change\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics spanning 1-D, subsequence, grid, and string DP.</blockquote>\n<hr>\n<h2 id=\"1-house-robber\">1. House Robber</h2>\n<p>Max sum of non-adjacent houses. State: <code>dp[i]</code> = best loot considering houses <code>0..i</code>. Transition: <code>dp[i] = max(dp[i-1], dp[i-2] + nums[i])</code> — skip house <code>i</code>, or rob it and add <code>dp[i-2]</code>. Only the last two values matter, so it's <code>O(1)</code> space.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def rob(nums: list[int]) -&gt; int:\n    prev2, prev1 = 0, 0                    # best up to i-2 and i-1\n    for x in nums:\n        prev2, prev1 = prev1, max(prev1, prev2 + x)\n    return prev1</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint rob(std::vector&lt;int&gt;&amp; nums) {\n    int prev2 = 0, prev1 = 0;\n    for (int x : nums) {\n        int cur = std::max(prev1, prev2 + x);\n        prev2 = prev1; prev1 = cur;\n    }\n    return prev1;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-longest-increasing-subsequence\">2. Longest Increasing Subsequence</h2>\n<p>Length of the longest strictly increasing subsequence. The <code>O(n log n)</code> method keeps <code>tails[k]</code> = the smallest possible tail of an increasing subsequence of length <code>k+1</code>; binary-search where each value extends or replaces a tail. The array's length is the answer.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from bisect import bisect_left\n\ndef length_of_lis(nums: list[int]) -&gt; int:\n    tails = []\n    for x in nums:\n        i = bisect_left(tails, x)          # first tail &gt;= x\n        if i == len(tails):\n            tails.append(x)                # x extends the longest chain\n        else:\n            tails[i] = x                   # x is a smaller tail for that length\n    return len(tails)</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint lengthOfLIS(std::vector&lt;int&gt;&amp; nums) {\n    std::vector&lt;int&gt; tails;\n    for (int x : nums) {\n        auto it = std::lower_bound(tails.begin(), tails.end(), x);\n        if (it == tails.end()) tails.push_back(x);\n        else *it = x;\n    }\n    return tails.size();\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-unique-paths\">3. Unique Paths</h2>\n<p>Number of paths from top-left to bottom-right of an <code>m × n</code> grid moving only right/down. Grid DP: <code>dp[c] = dp[c] + dp[c-1]</code> (paths from above + paths from the left), rolled into a single row for <code>O(n)</code> space.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def unique_paths(m: int, n: int) -&gt; int:\n    dp = [1] * n                           # first row: one way to each cell\n    for _ in range(1, m):\n        for c in range(1, n):\n            dp[c] += dp[c - 1]             # from above (old dp[c]) + from left\n    return dp[-1]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nint uniquePaths(int m, int n) {\n    std::vector&lt;int&gt; dp(n, 1);\n    for (int r = 1; r &lt; m; r++)\n        for (int c = 1; c &lt; n; c++)\n            dp[c] += dp[c - 1];\n    return dp[n - 1];\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-word-break\">4. Word Break</h2>\n<p>Can <code>s</code> be segmented into words from a dictionary? State: <code>dp[i]</code> = \"is <code>s[:i]</code> segmentable?\". Transition: <code>dp[i]</code> is true if some <code>j &lt; i</code> has <code>dp[j]</code> true and <code>s[j:i]</code> is a word. <strong>O(n²)</strong> time (with a set for <code>O(1)</code> lookups).</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def word_break(s: str, word_dict: list[str]) -&gt; bool:\n    words = set(word_dict)\n    dp = [True] + [False] * len(s)         # dp[0]: empty prefix is segmentable\n    for i in range(1, len(s) + 1):\n        for j in range(i):\n            if dp[j] and s[j:i] in words:\n                dp[i] = True\n                break\n    return dp[len(s)]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;string&gt;\n#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n\nbool wordBreak(std::string s, std::vector&lt;std::string&gt;&amp; wordDict) {\n    std::unordered_set&lt;std::string&gt; words(wordDict.begin(), wordDict.end());\n    int n = s.size();\n    std::vector&lt;bool&gt; dp(n + 1, false);\n    dp[0] = true;\n    for (int i = 1; i &lt;= n; i++)\n        for (int j = 0; j &lt; i; j++)\n            if (dp[j] &amp;&amp; words.count(s.substr(j, i - j))) { dp[i] = true; break; }\n    return dp[n];\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>How do you turn a brute-force recursion into a DP?</summary><div class=\"qa-body\"><p>Write the recursion first (define what the function returns for a given state). Draw the tree; if the <strong>same arguments</strong> recur, the subproblems overlap, so add a <strong>cache keyed by the state</strong> — that's top-down memoization and it collapses exponential work to polynomial. If you also want the constant-factor and space win, rewrite it <strong>bottom-up</strong>: enumerate states in dependency order and fill a table, then compress dimensions you no longer need (House Robber → two scalars, Unique Paths → one row).</p></div></details>\n<details class=\"qa\"><summary>DP vs. greedy — when is greedy <em>not</em> safe?</summary><div class=\"qa-body\"><p>Greedy commits to a locally optimal choice and never reconsiders; it's correct only when an <strong>exchange argument</strong> proves a greedy-consistent global optimum exists. When a locally worse choice can enable a better overall outcome (Coin Change with arbitrary denominations, 0/1 knapsack), greedy fails and you need DP, which considers all choices via overlapping subproblems.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. House Robber",
        "id": "1-house-robber"
      },
      {
        "level": 2,
        "txt": "2. Longest Increasing Subsequence",
        "id": "2-longest-increasing-subsequence"
      },
      {
        "level": 2,
        "txt": "3. Unique Paths",
        "id": "3-unique-paths"
      },
      {
        "level": 2,
        "txt": "4. Word Break",
        "id": "4-word-break"
      }
    ]
  },
  {
    "slug": "backtracking",
    "title": "Backtracking — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>DFS over a tree of partial solutions: <strong>choose → explore → un-choose</strong>. Prune branches that can't lead to a valid/better answer. Exponential in the worst case, but pruning and the choose/undo discipline make it practical and the code uniform.</p>\n<p><strong>Use when:</strong> generate all subsets/permutations/combinations, N-queens, Sudoku, word search, partitioning.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-backtracking.svg\" alt=\"Backtracking decision tree for subsets of {1,2,3}\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function backtrack(partial):\n    if partial is a complete solution:\n        record(partial); return\n    for choice in choices(partial):\n        if not promising(choice): continue    # prune dead branches early\n        apply(choice)                          # choose\n        backtrack(partial + choice)            # explore\n        undo(choice)                           # un-choose</code></pre>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>def subsets(nums):\n    res, path = [], []\n    def backtrack(start):\n        res.append(path[:])              # every node is a valid subset\n        for i in range(start, len(nums)):\n            path.append(nums[i])          # choose\n            backtrack(i + 1)              # explore (i+1 → no reuse; i → allow reuse)\n            path.pop()                    # un-choose\n    backtrack(0)\n    return res</code></pre>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> the cleanest choose/explore/un-choose template, <strong>Subsets</strong>, runs in the editor → <a href=\"practice.html?p=subsets\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics.</blockquote>\n<hr>\n<h2 id=\"1-permutations\">1. Permutations</h2>\n<p>All orderings of distinct <code>nums</code>. At each depth, choose any unused element, recurse, then undo. A <code>used</code> array (or swapping in place) tracks what's already placed. <strong>O(n · n!)</strong> outputs.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def permute(nums: list[int]) -&gt; list[list[int]]:\n    res, path = [], []\n    used = [False] * len(nums)\n    def backtrack():\n        if len(path) == len(nums):\n            res.append(path[:])            # a complete permutation\n            return\n        for i in range(len(nums)):\n            if used[i]: continue\n            used[i] = True; path.append(nums[i])    # choose\n            backtrack()                              # explore\n            path.pop(); used[i] = False              # un-choose\n    backtrack()\n    return res</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n\nvoid backtrack(std::vector&lt;int&gt;&amp; nums, std::vector&lt;bool&gt;&amp; used,\n               std::vector&lt;int&gt;&amp; path, std::vector&lt;std::vector&lt;int&gt;&gt;&amp; res) {\n    if (path.size() == nums.size()) { res.push_back(path); return; }\n    for (int i = 0; i &lt; (int)nums.size(); i++) {\n        if (used[i]) continue;\n        used[i] = true; path.push_back(nums[i]);\n        backtrack(nums, used, path, res);\n        path.pop_back(); used[i] = false;\n    }\n}\nstd::vector&lt;std::vector&lt;int&gt;&gt; permute(std::vector&lt;int&gt;&amp; nums) {\n    std::vector&lt;std::vector&lt;int&gt;&gt; res;\n    std::vector&lt;int&gt; path;\n    std::vector&lt;bool&gt; used(nums.size(), false);\n    backtrack(nums, used, path, res);\n    return res;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-combination-sum\">2. Combination Sum</h2>\n<p>All combinations of <code>candidates</code> (reuse allowed) that sum to <code>target</code>. Pass <code>start</code> so combinations stay non-decreasing (no permuted duplicates); recurse from <code>i</code> (not <code>i+1</code>) to allow reuse; prune when the remaining target goes negative.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def combination_sum(candidates: list[int], target: int) -&gt; list[list[int]]:\n    res, path = [], []\n    def backtrack(start, remain):\n        if remain == 0:\n            res.append(path[:]); return\n        for i in range(start, len(candidates)):\n            if candidates[i] &gt; remain:     # (sort first to make this a hard prune)\n                continue\n            path.append(candidates[i])\n            backtrack(i, remain - candidates[i])   # i, not i+1 → reuse allowed\n            path.pop()\n    candidates.sort()\n    backtrack(0, target)\n    return res</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nvoid backtrack(std::vector&lt;int&gt;&amp; c, int start, int remain,\n               std::vector&lt;int&gt;&amp; path, std::vector&lt;std::vector&lt;int&gt;&gt;&amp; res) {\n    if (remain == 0) { res.push_back(path); return; }\n    for (int i = start; i &lt; (int)c.size(); i++) {\n        if (c[i] &gt; remain) break;          // sorted → all later are too big\n        path.push_back(c[i]);\n        backtrack(c, i, remain - c[i], path, res);\n        path.pop_back();\n    }\n}\nstd::vector&lt;std::vector&lt;int&gt;&gt; combinationSum(std::vector&lt;int&gt;&amp; candidates, int target) {\n    std::sort(candidates.begin(), candidates.end());\n    std::vector&lt;std::vector&lt;int&gt;&gt; res;\n    std::vector&lt;int&gt; path;\n    backtrack(candidates, 0, target, path, res);\n    return res;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-word-search\">3. Word Search</h2>\n<p>Does <code>word</code> exist in the grid along 4-directional adjacent cells (no reuse)? DFS from each cell, marking the current cell visited (mutate then restore) as you match characters. <strong>O(R·C·4^L)</strong> worst case.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def exist(board: list[list[str]], word: str) -&gt; bool:\n    R, C = len(board), len(board[0])\n    def dfs(r, c, k):\n        if k == len(word): return True\n        if not (0 &lt;= r &lt; R and 0 &lt;= c &lt; C) or board[r][c] != word[k]:\n            return False\n        tmp, board[r][c] = board[r][c], \"#\"        # mark visited\n        found = (dfs(r+1,c,k+1) or dfs(r-1,c,k+1) or\n                 dfs(r,c+1,k+1) or dfs(r,c-1,k+1))\n        board[r][c] = tmp                            # restore\n        return found\n    return any(dfs(r, c, 0) for r in range(R) for c in range(C))</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;string&gt;\n\nbool dfs(std::vector&lt;std::vector&lt;char&gt;&gt;&amp; b, const std::string&amp; w, int r, int c, int k) {\n    if (k == (int)w.size()) return true;\n    int R = b.size(), C = b[0].size();\n    if (r &lt; 0 || r &gt;= R || c &lt; 0 || c &gt;= C || b[r][c] != w[k]) return false;\n    char tmp = b[r][c]; b[r][c] = '#';\n    bool found = dfs(b,w,r+1,c,k+1) || dfs(b,w,r-1,c,k+1) ||\n                 dfs(b,w,r,c+1,k+1) || dfs(b,w,r,c-1,k+1);\n    b[r][c] = tmp;\n    return found;\n}\nbool exist(std::vector&lt;std::vector&lt;char&gt;&gt;&amp; board, std::string word) {\n    for (int r = 0; r &lt; (int)board.size(); r++)\n        for (int c = 0; c &lt; (int)board[0].size(); c++)\n            if (dfs(board, word, r, c, 0)) return true;\n    return false;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>What's the core template, and where does the pruning live?</summary><div class=\"qa-body\"><p><code>choose → explore → un-choose</code>: apply a choice, recurse, then <strong>undo it</strong> so siblings start from a clean state. Pruning lives at the top of each recursive call (or before recursing): reject partial states that can't complete (<code>remain &lt; 0</code>, character mismatch, constraint violated). Good pruning is what turns a theoretically exponential search into something that finishes.</p></div></details>\n<details class=\"qa\"><summary>How do you avoid duplicate combinations vs. duplicate permutations?</summary><div class=\"qa-body\"><p>For <strong>combinations</strong>, pass a <code>start</code> index and only pick elements at or after it, so each set is generated in one canonical (non-decreasing) order. For <strong>permutations with duplicate values</strong>, sort first and skip a value at a given depth if it equals the previous value and that previous copy wasn't used (<code>i &gt; 0 &amp;&amp; nums[i] == nums[i-1] &amp;&amp; !used[i-1]</code>), which cuts mirrored branches.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Permutations",
        "id": "1-permutations"
      },
      {
        "level": 2,
        "txt": "2. Combination Sum",
        "id": "2-combination-sum"
      },
      {
        "level": 2,
        "txt": "3. Word Search",
        "id": "3-word-search"
      }
    ]
  },
  {
    "slug": "heap",
    "title": "Heap / Top-K — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>A <strong>heap (priority queue)</strong> gives <code>O(log n)</code> push/pop of the min (or max) and <code>O(1)</code> peek. Use it for <strong>top-k</strong>, <strong>merge k sorted lists</strong>, <strong>running median</strong> (two heaps), and Dijkstra/Prim. In Python, <code>heapq</code> is a <strong>min-heap</strong> — push negatives (or tuples) for max-heap behavior; in C++, <code>std::priority_queue</code> is a <strong>max-heap</strong> by default.</p>\n<p><strong>Use when:</strong> \"k largest / smallest\", \"median of a stream\", \"k closest\", or \"always pull the current best/worst\".</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-heap.svg\" alt=\"Min-heap as tree and array, plus the top-k trick\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (top-k with a heap):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function topK(items, k):\n    heap ← empty min-heap\n    for x in items:\n        heap.push(x)\n        if heap.size &gt; k:\n            heap.pop()            # drop the smallest → heap keeps the k largest\n    return heap                   # O(n log k) time, O(k) space</code></pre>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> <strong>Top K Frequent Elements</strong> runs in the editor → <a href=\"practice.html?p=top-k-frequent\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics.</blockquote>\n<hr>\n<h2 id=\"1-kth-largest-element-in-an-array-heap\">1. Kth Largest Element in an Array (heap)</h2>\n<p>Keep a <strong>size-k min-heap</strong>: push each value, pop the smallest whenever the heap exceeds <code>k</code>. The heap's root is then the k-th largest. <strong>O(n log k)</strong> time, <strong>O(k)</strong> space — cheaper than sorting when <code>k ≪ n</code>.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import heapq\n\ndef find_kth_largest(nums: list[int], k: int) -&gt; int:\n    heap = []\n    for x in nums:\n        heapq.heappush(heap, x)\n        if len(heap) &gt; k:\n            heapq.heappop(heap)            # drop the smallest → keep k largest\n    return heap[0]                          # root = k-th largest</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;queue&gt;\n\nint findKthLargest(std::vector&lt;int&gt;&amp; nums, int k) {\n    std::priority_queue&lt;int, std::vector&lt;int&gt;, std::greater&lt;int&gt;&gt; heap;  // min-heap\n    for (int x : nums) {\n        heap.push(x);\n        if ((int)heap.size() &gt; k) heap.pop();\n    }\n    return heap.top();\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-k-closest-points-to-origin\">2. K Closest Points to Origin</h2>\n<p>Return the <code>k</code> points nearest the origin. Same size-k heap idea, ordered by squared distance (no <code>sqrt</code> needed) — but as a <strong>max-heap</strong> so the farthest of the current best-k sits on top and gets evicted. <strong>O(n log k)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import heapq\n\ndef k_closest(points: list[list[int]], k: int) -&gt; list[list[int]]:\n    heap = []                              # max-heap via negated distance\n    for x, y in points:\n        d = x * x + y * y\n        heapq.heappush(heap, (-d, x, y))\n        if len(heap) &gt; k:\n            heapq.heappop(heap)            # evict the farthest\n    return [[x, y] for _, x, y in heap]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;queue&gt;\n\nstd::vector&lt;std::vector&lt;int&gt;&gt; kClosest(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; points, int k) {\n    std::priority_queue&lt;std::pair&lt;int, int&gt;&gt; heap;   // max-heap keyed by distance, value = index\n    for (int i = 0; i &lt; (int)points.size(); i++) {\n        int d = points[i][0]*points[i][0] + points[i][1]*points[i][1];\n        heap.push({d, i});\n        if ((int)heap.size() &gt; k) heap.pop();         // evict farthest\n    }\n    std::vector&lt;std::vector&lt;int&gt;&gt; res;\n    while (!heap.empty()) { res.push_back(points[heap.top().second]); heap.pop(); }\n    return res;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-find-median-from-data-stream\">3. Find Median from Data Stream</h2>\n<p>Maintain <strong>two heaps</strong>: a max-heap <code>lo</code> for the smaller half and a min-heap <code>hi</code> for the larger half, kept balanced in size. The median is <code>lo</code>'s top (odd total) or the average of both tops (even). Each <code>addNum</code> is <code>O(log n)</code>; <code>findMedian</code> is <code>O(1)</code>.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import heapq\n\nclass MedianFinder:\n    def __init__(self):\n        self.lo = []                       # max-heap (negated) for the smaller half\n        self.hi = []                       # min-heap for the larger half\n\n    def addNum(self, num: int) -&gt; None:\n        heapq.heappush(self.lo, -num)\n        heapq.heappush(self.hi, -heapq.heappop(self.lo))   # move largest of lo to hi\n        if len(self.hi) &gt; len(self.lo):                     # rebalance so len(lo) &gt;= len(hi)\n            heapq.heappush(self.lo, -heapq.heappop(self.hi))\n\n    def findMedian(self) -&gt; float:\n        if len(self.lo) &gt; len(self.hi):\n            return -self.lo[0]\n        return (-self.lo[0] + self.hi[0]) / 2</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;queue&gt;\n\nclass MedianFinder {\n    std::priority_queue&lt;int&gt; lo;                                   // max-heap, smaller half\n    std::priority_queue&lt;int, std::vector&lt;int&gt;, std::greater&lt;int&gt;&gt; hi;  // min-heap, larger half\npublic:\n    void addNum(int num) {\n        lo.push(num);\n        hi.push(lo.top()); lo.pop();               // shift largest of lo to hi\n        if (hi.size() &gt; lo.size()) { lo.push(hi.top()); hi.pop(); }\n    }\n    double findMedian() {\n        if (lo.size() &gt; hi.size()) return lo.top();\n        return (lo.top() + hi.top()) / 2.0;\n    }\n};</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Why does a size-k heap beat sorting for \"k largest\"?</summary><div class=\"qa-body\"><p>Sorting is <code>O(n log n)</code>; a size-k heap is <code>O(n log k)</code>. When <code>k</code> is small relative to <code>n</code>, <code>log k ≪ log n</code>, so you do far less work — and you only hold <code>k</code> elements in memory, which matters for <strong>streams</strong> where you can't store all <code>n</code>. You keep the k best seen so far and evict the worst of them as better ones arrive.</p></div></details>\n<details class=\"qa\"><summary>For the median stream, why two heaps instead of one sorted structure?</summary><div class=\"qa-body\"><p>Insertion into a sorted array is <code>O(n)</code> per element. Two heaps give <code>O(log n)</code> insertion while keeping the boundary between the halves <code>O(1)</code> to read: the max-heap's top is the largest of the low half, the min-heap's top the smallest of the high half. Keep their sizes within one of each other and the median is always one or two tops away.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Kth Largest Element in an Array (heap)",
        "id": "1-kth-largest-element-in-an-array-heap"
      },
      {
        "level": 2,
        "txt": "2. K Closest Points to Origin",
        "id": "2-k-closest-points-to-origin"
      },
      {
        "level": 2,
        "txt": "3. Find Median from Data Stream",
        "id": "3-find-median-from-data-stream"
      }
    ]
  },
  {
    "slug": "union-find",
    "title": "Union-Find — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p><strong>Union-find (disjoint set union)</strong> gives near-<code>O(1)</code> <code>union</code>/<code>find</code> with <strong>path compression</strong> + <strong>union by rank/size</strong>. It answers \"are these two in the same group?\" and \"how many groups?\" under incremental merges — the go-to for <strong>dynamic connectivity</strong>, counting components, cycle detection in an <strong>undirected</strong> graph, and Kruskal's MST.</p>\n<p><strong>Use when:</strong> you're repeatedly merging pairs and asking about connectivity or group counts, or a single extra edge creates a cycle.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-union-find.svg\" alt=\"Union-find forests merging two sets\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>parent[i] ← i for every i\nfunction find(x):\n    while parent[x] ≠ x:\n        parent[x] ← parent[parent[x]]    # path compression\n        x ← parent[x]\n    return x\nfunction union(a, b):\n    ra ← find(a);  rb ← find(b)\n    if ra == rb: return false            # same set → this edge closes a cycle\n    parent[rb] ← ra                      # (attach smaller rank under larger)\n    return true</code></pre>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> <strong>Number of Provinces</strong> runs in the editor → <a href=\"practice.html?p=number-of-provinces\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics — each reuses this DSU:</blockquote>\n<pre class=\"lec-pre\" data-lang=\"python\"><code>class DSU:\n    def __init__(self, n):\n        self.p = list(range(n)); self.r = [0] * n\n    def find(self, x):\n        while self.p[x] != x:\n            self.p[x] = self.p[self.p[x]]     # path compression (halving)\n            x = self.p[x]\n        return x\n    def union(self, a, b):\n        ra, rb = self.find(a), self.find(b)\n        if ra == rb: return False             # already connected → this edge closes a cycle\n        if self.r[ra] &lt; self.r[rb]: ra, rb = rb, ra\n        self.p[rb] = ra\n        if self.r[ra] == self.r[rb]: self.r[ra] += 1\n        return True</code></pre>\n<hr>\n<h2 id=\"1-redundant-connection\">1. Redundant Connection</h2>\n<p>A tree had one extra edge added, forming a single cycle; return the edge that can be removed (the last one in the input that closes a cycle). Union each edge — the first <code>union</code> that returns false is the redundant edge. <strong>O(n · α(n))</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def find_redundant_connection(edges: list[list[int]]) -&gt; list[int]:\n    dsu = DSU(len(edges) + 1)               # nodes are 1..n\n    for a, b in edges:\n        if not dsu.union(a, b):             # a and b already connected → cycle edge\n            return [a, b]\n    return []</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;numeric&gt;\n\nstruct DSU {\n    std::vector&lt;int&gt; p, r;\n    DSU(int n) : p(n), r(n, 0) { std::iota(p.begin(), p.end(), 0); }\n    int find(int x) { while (p[x] != x) { p[x] = p[p[x]]; x = p[x]; } return x; }\n    bool unite(int a, int b) {\n        int ra = find(a), rb = find(b);\n        if (ra == rb) return false;\n        if (r[ra] &lt; r[rb]) std::swap(ra, rb);\n        p[rb] = ra;\n        if (r[ra] == r[rb]) r[ra]++;\n        return true;\n    }\n};\n\nstd::vector&lt;int&gt; findRedundantConnection(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; edges) {\n    DSU dsu(edges.size() + 1);\n    for (auto&amp; e : edges)\n        if (!dsu.unite(e[0], e[1])) return e;\n    return {};\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-number-of-connected-components-in-an-undirected-graph\">2. Number of Connected Components in an Undirected Graph</h2>\n<p>Count groups after unioning all edges. Start with <code>n</code> components and decrement each time a <code>union</code> actually merges two different sets. <strong>O(n + E·α(n))</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def count_components(n: int, edges: list[list[int]]) -&gt; int:\n    dsu = DSU(n)\n    count = n\n    for a, b in edges:\n        if dsu.union(a, b):                 # a real merge reduces the count\n            count -= 1\n    return count</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;numeric&gt;\n\nint countComponents(int n, std::vector&lt;std::vector&lt;int&gt;&gt;&amp; edges) {\n    DSU dsu(n);\n    int count = n;\n    for (auto&amp; e : edges)\n        if (dsu.unite(e[0], e[1])) count--;\n    return count;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-accounts-merge\">3. Accounts Merge</h2>\n<p>Merge accounts that share any email (same person). Map each email to an owner index, union accounts sharing an email, then group emails by their set's root. Sort each group and prepend the name. Union-find handles the transitive \"A shares with B, B shares with C\" merging cleanly.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def accounts_merge(accounts: list[list[str]]) -&gt; list[list[str]]:\n    dsu = DSU(len(accounts))\n    owner = {}                              # email -&gt; first account index seen\n    for i, acc in enumerate(accounts):\n        for email in acc[1:]:\n            if email in owner:\n                dsu.union(i, owner[email])  # same email → same person\n            else:\n                owner[email] = i\n    groups = {}                             # root index -&gt; set of emails\n    for email, i in owner.items():\n        root = dsu.find(i)\n        groups.setdefault(root, set()).add(email)\n    return [[accounts[root][0]] + sorted(emails) for root, emails in groups.items()]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;unordered_map&gt;\n#include &lt;map&gt;\n#include &lt;set&gt;\n#include &lt;numeric&gt;\n\nstd::vector&lt;std::vector&lt;std::string&gt;&gt; accountsMerge(std::vector&lt;std::vector&lt;std::string&gt;&gt;&amp; accounts) {\n    DSU dsu(accounts.size());\n    std::unordered_map&lt;std::string, int&gt; owner;\n    for (int i = 0; i &lt; (int)accounts.size(); i++)\n        for (int j = 1; j &lt; (int)accounts[i].size(); j++) {\n            auto&amp; email = accounts[i][j];\n            if (owner.count(email)) dsu.unite(i, owner[email]);\n            else owner[email] = i;\n        }\n    std::map&lt;int, std::set&lt;std::string&gt;&gt; groups;\n    for (auto&amp; [email, i] : owner) groups[dsu.find(i)].insert(email);\n    std::vector&lt;std::vector&lt;std::string&gt;&gt; res;\n    for (auto&amp; [root, emails] : groups) {\n        std::vector&lt;std::string&gt; row{accounts[root][0]};\n        row.insert(row.end(), emails.begin(), emails.end());\n        res.push_back(row);\n    }\n    return res;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Why is union-find preferred over DFS/BFS for connectivity?</summary><div class=\"qa-body\"><p>When connectivity is queried <strong>incrementally</strong> as edges arrive (or you just need group counts after many merges), DSU answers each <code>union</code>/<code>find</code> in near-<code>O(1)</code> amortized without rebuilding a traversal. DFS/BFS recompute components from scratch per query. DSU is also the natural fit for <strong>Kruskal's MST</strong> (add an edge iff it joins two different components) and undirected cycle detection.</p></div></details>\n<details class=\"qa\"><summary>What do path compression and union by rank each buy you?</summary><div class=\"qa-body\"><p><strong>Path compression</strong> flattens the tree during <code>find</code>, pointing nodes directly (or nearly) at the root so later <code>find</code>s are shallow. <strong>Union by rank/size</strong> always hangs the smaller tree under the larger root, keeping trees shallow to begin with. Together they give the inverse-Ackermann bound <code>O(α(n))</code> per operation — effectively constant. Either alone is worse.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Redundant Connection",
        "id": "1-redundant-connection"
      },
      {
        "level": 2,
        "txt": "2. Number of Connected Components in an Undirected Graph",
        "id": "2-number-of-connected-components-in-an-undirected-graph"
      },
      {
        "level": 2,
        "txt": "3. Accounts Merge",
        "id": "3-accounts-merge"
      }
    ]
  },
  {
    "slug": "stack",
    "title": "Stack & Monotonic Stack — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>A <strong>stack</strong> is a LIFO (\"last in, first out\") container: you only ever touch the <strong>top</strong>. <code>push</code>, <code>pop</code>, <code>peek</code>, <code>isEmpty</code>, and <code>size</code> are all <strong>O(1)</strong>. In Python a plain list <em>is</em> a stack — <code>append</code> to push, <code>pop()</code> to pop, <code>a[-1]</code> to peek. Reach for it whenever the <strong>most recent</strong> unresolved thing is the one you need next: matching brackets, undo/redo, DFS's explicit frontier, expression evaluation, or \"what happened just before this.\"</p>\n<p><strong>Use when:</strong> balanced-brackets / nesting validation, evaluating or parsing expressions (RPN, calculators), backtracking an explicit call stack, or the <strong>next greater/smaller element</strong> family (the monotonic-stack special case below).</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-stack.svg\" alt=\"Stack LIFO operations and a decreasing monotonic stack resolving next-greater-element\" loading=\"lazy\"></p>\n<p><strong>Pseudocode — balanced brackets (the \"match the most recent open\" reflex):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function isBalanced(s):\n    stack ← empty\n    pairs ← { ')':'(', ']':'[', '}':'{' }\n    for ch in s:\n        if ch is an opening bracket:\n            stack.push(ch)\n        else:                                 # closing bracket\n            if stack empty or stack.pop() ≠ pairs[ch]:\n                return false                  # mismatch or nothing to close\n    return stack is empty                     # nothing left dangling</code></pre>\n<p><strong>Monotonic stack — the \"next greater/smaller element\" pattern.</strong> Keep the stack's values in sorted (increasing <em>or</em> decreasing) order by <strong>popping violators before you push</strong>. Each index is pushed and popped <strong>at most once → O(n)</strong> for a whole class of problems that look <code>O(n²)</code>. Store <strong>indices</strong> (not just values) when you need distances. A <strong>decreasing</strong> stack finds <em>next greater</em>; an <strong>increasing</strong> stack finds <em>next smaller</em>.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function nextGreater(a):                       # decreasing monotonic stack of indices\n    res ← array of 0s, size len(a)\n    stack ← empty\n    for i, x in enumerate(a):\n        while stack not empty and a[stack.top] &lt; x:\n            j ← stack.pop()\n            res[j] ← i − j                     # x at i is j's next-greater\n        stack.push(i)\n    return res                                 # unresolved indices keep their 0</code></pre>\n<p>Same skeleton powers <strong>Next Greater Element</strong>, <strong>Largest Rectangle in Histogram</strong>, <strong>Trapping Rain Water</strong>, and stock-span problems — swap the comparison and what you record on each pop.</p>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> the monotonic-deque cousin, <strong>Sliding Window Maximum</strong>, runs in the editor → <a href=\"practice.html?p=sliding-window-maximum\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics.</blockquote>\n<hr>\n<h2 id=\"1-valid-parentheses\">1. Valid Parentheses</h2>\n<p>Are the brackets balanced and correctly nested? Push openers; on a closer, the top must be its matching opener — otherwise it's invalid. Valid iff the stack ends empty. <strong>O(n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def is_valid(s: str) -&gt; bool:\n    pairs = {\")\": \"(\", \"]\": \"[\", \"}\": \"{\"}\n    stack = []\n    for ch in s:\n        if ch not in pairs:\n            stack.append(ch)                # opener → push\n        elif not stack or stack.pop() != pairs[ch]:\n            return False                     # closer with wrong/absent match\n    return not stack                          # nothing left dangling</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;string&gt;\n#include &lt;stack&gt;\n#include &lt;unordered_map&gt;\n\nbool isValid(std::string s) {\n    std::unordered_map&lt;char, char&gt; pairs = {{')','('}, {']','['}, {'}','{'}};\n    std::stack&lt;char&gt; st;\n    for (char ch : s) {\n        if (!pairs.count(ch)) st.push(ch);\n        else {\n            if (st.empty() || st.top() != pairs[ch]) return false;\n            st.pop();\n        }\n    }\n    return st.empty();\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-daily-temperatures\">2. Daily Temperatures</h2>\n<p>For each day, how many days until a warmer one. A <strong>decreasing</strong> monotonic stack of indices: when today is warmer than the top, pop and record the distance. Unresolved days keep 0. <strong>O(n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def daily_temperatures(temps: list[int]) -&gt; list[int]:\n    res = [0] * len(temps)\n    stack = []                              # indices, temps decreasing down the stack\n    for i, t in enumerate(temps):\n        while stack and temps[stack[-1]] &lt; t:\n            j = stack.pop()\n            res[j] = i - j                  # i is the first warmer day for day j\n        stack.append(i)\n    return res</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;stack&gt;\n\nstd::vector&lt;int&gt; dailyTemperatures(std::vector&lt;int&gt;&amp; temps) {\n    std::vector&lt;int&gt; res(temps.size(), 0);\n    std::stack&lt;int&gt; st;                     // indices, decreasing temps\n    for (int i = 0; i &lt; (int)temps.size(); i++) {\n        while (!st.empty() &amp;&amp; temps[st.top()] &lt; temps[i]) {\n            int j = st.top(); st.pop();\n            res[j] = i - j;\n        }\n        st.push(i);\n    }\n    return res;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-largest-rectangle-in-histogram\">3. Largest Rectangle in Histogram</h2>\n<p>Largest axis-aligned rectangle under the bar heights. An <strong>increasing</strong> monotonic stack of indices: when a shorter bar appears, pop taller bars and compute each popped bar's maximal rectangle (its height × the width between the new boundary and the previous stack entry). A sentinel <code>0</code> at the end flushes the stack. <strong>O(n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def largest_rectangle_area(heights: list[int]) -&gt; int:\n    stack = []                              # indices, increasing heights\n    best = 0\n    for i, h in enumerate(heights + [0]):   # trailing 0 flushes the stack\n        while stack and heights[stack[-1]] &gt;= h:\n            top = stack.pop()\n            height = heights[top]\n            width = i if not stack else i - stack[-1] - 1\n            best = max(best, height * width)\n        stack.append(i)\n    return best</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;stack&gt;\n#include &lt;algorithm&gt;\n\nint largestRectangleArea(std::vector&lt;int&gt;&amp; heights) {\n    heights.push_back(0);                   // sentinel flushes the stack\n    std::stack&lt;int&gt; st;\n    int best = 0;\n    for (int i = 0; i &lt; (int)heights.size(); i++) {\n        while (!st.empty() &amp;&amp; heights[st.top()] &gt;= heights[i]) {\n            int h = heights[st.top()]; st.pop();\n            int width = st.empty() ? i : i - st.top() - 1;\n            best = std::max(best, h * width);\n        }\n        st.push(i);\n    }\n    heights.pop_back();\n    return best;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-min-stack\">4. Min Stack</h2>\n<p>A stack with <code>push</code>/<code>pop</code>/<code>top</code>/<code>getMin</code> all <code>O(1)</code>. Keep an <strong>auxiliary stack of running minima</strong> alongside the data stack; each entry records the min of everything at or below it.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class MinStack:\n    def __init__(self):\n        self.stack = []\n        self.mins = []                      # mins[i] = min of stack[0..i]\n\n    def push(self, val: int) -&gt; None:\n        self.stack.append(val)\n        self.mins.append(val if not self.mins else min(val, self.mins[-1]))\n\n    def pop(self) -&gt; None:\n        self.stack.pop(); self.mins.pop()\n\n    def top(self) -&gt; int:\n        return self.stack[-1]\n\n    def getMin(self) -&gt; int:\n        return self.mins[-1]</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;stack&gt;\n#include &lt;algorithm&gt;\n\nclass MinStack {\n    std::stack&lt;int&gt; stk, mins;\npublic:\n    void push(int val) {\n        stk.push(val);\n        mins.push(mins.empty() ? val : std::min(val, mins.top()));\n    }\n    void pop() { stk.pop(); mins.pop(); }\n    int top() { return stk.top(); }\n    int getMin() { return mins.top(); }\n};</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>Why is a monotonic-stack scan O(n) despite the inner while-loop?</summary><div class=\"qa-body\"><p>Because each index is <strong>pushed exactly once and popped at most once</strong> across the entire scan. The inner <code>while</code> can do many pops in one iteration, but the total number of pops over all iterations can't exceed the total number of pushes (<code>n</code>). So the combined work is <code>O(n)</code> — an amortized argument, not a per-step one.</p></div></details>\n<details class=\"qa\"><summary>Decreasing vs. increasing monotonic stack — which finds what?</summary><div class=\"qa-body\"><p>A <strong>decreasing</strong> stack (values shrink toward the top) resolves <strong>next greater element</strong>: a new larger value pops everything smaller, and it's their next-greater. An <strong>increasing</strong> stack resolves <strong>next smaller element</strong> and underlies Largest Rectangle in Histogram. Store <strong>indices</strong> rather than values whenever the answer is a distance or width.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Valid Parentheses",
        "id": "1-valid-parentheses"
      },
      {
        "level": 2,
        "txt": "2. Daily Temperatures",
        "id": "2-daily-temperatures"
      },
      {
        "level": 2,
        "txt": "3. Largest Rectangle in Histogram",
        "id": "3-largest-rectangle-in-histogram"
      },
      {
        "level": 2,
        "txt": "4. Min Stack",
        "id": "4-min-stack"
      }
    ]
  },
  {
    "slug": "greedy",
    "title": "Greedy — common interview questions",
    "html": "<h2 id=\"how-it-works\">How it works</h2>\n<p>Make the <strong>locally optimal choice</strong> at each step and never reconsider it. Fast (usually one pass or one sort) — but correct <strong>only when the greedy choice is provably part of some global optimum</strong>. If you can't argue that (usually via an <strong>exchange argument</strong>), fall back to DP.</p>\n<p><strong>Use when:</strong> interval scheduling (earliest finish time), \"reach the end / minimum jumps,\" Huffman coding, assigning/partitioning where an exchange argument holds.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-greedy.svg\" alt=\"Greedy reachability in Jump Game\" loading=\"lazy\"></p>\n<p><strong>Pseudocode (template):</strong></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>function greedy(items):\n    sort / order items by the greedy key      # e.g. earliest finish, smallest cost\n    answer ← initial\n    for item in items:\n        if item is compatible with the choices so far:\n            take item; update answer           # commit, never undo\n    return answer</code></pre>\n<blockquote class=\"lec-callout\">Proving it: assume an optimal solution differs from the greedy one, then <strong>exchange</strong> the first differing choice for the greedy choice without making the solution worse — so a greedy-consistent optimum exists.</blockquote>\n<blockquote class=\"lec-callout\"><strong>In-site drill:</strong> <strong>Jump Game</strong> (farthest-reachable in one pass) runs in the editor → <a href=\"practice.html?p=jump-game\" target=\"_blank\" rel=\"noopener\">open it</a>. Below are additional classics.</blockquote>\n<hr>\n<h2 id=\"1-gas-station\">1. Gas Station</h2>\n<p>Find the start index from which you can complete the loop, or <code>-1</code>. If total gas ≥ total cost a solution exists and is <strong>unique</strong>; track a running tank, and whenever it goes negative, no start in the stretch just covered can work — restart from the next station. <strong>O(n)</strong> time, one pass.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def can_complete_circuit(gas: list[int], cost: list[int]) -&gt; int:\n    if sum(gas) &lt; sum(cost):\n        return -1                           # not enough fuel overall\n    tank = start = 0\n    for i in range(len(gas)):\n        tank += gas[i] - cost[i]\n        if tank &lt; 0:                        # can't reach i+1 from `start`\n            start = i + 1                   # every station in [start..i] fails too\n            tank = 0\n    return start</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;numeric&gt;\n\nint canCompleteCircuit(std::vector&lt;int&gt;&amp; gas, std::vector&lt;int&gt;&amp; cost) {\n    int total = 0, tank = 0, start = 0;\n    for (int i = 0; i &lt; (int)gas.size(); i++) {\n        int diff = gas[i] - cost[i];\n        total += diff; tank += diff;\n        if (tank &lt; 0) { start = i + 1; tank = 0; }\n    }\n    return total &lt; 0 ? -1 : start;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"2-jump-game-ii\">2. Jump Game II</h2>\n<p>Minimum jumps to reach the last index (a jump is guaranteed possible). Greedily expand the current reachable \"level\": within <code>[left, right]</code>, compute the farthest reachable; when you pass the level's end, increment the jump count and start a new level. <strong>O(n)</strong> time — a BFS-in-disguise.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def jump(nums: list[int]) -&gt; int:\n    jumps = cur_end = farthest = 0\n    for i in range(len(nums) - 1):          # no need to jump from the last index\n        farthest = max(farthest, i + nums[i])\n        if i == cur_end:                    # exhausted the current jump's range\n            jumps += 1\n            cur_end = farthest\n    return jumps</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nint jump(std::vector&lt;int&gt;&amp; nums) {\n    int jumps = 0, curEnd = 0, farthest = 0;\n    for (int i = 0; i &lt; (int)nums.size() - 1; i++) {\n        farthest = std::max(farthest, i + nums[i]);\n        if (i == curEnd) { jumps++; curEnd = farthest; }\n    }\n    return jumps;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"3-merge-intervals\">3. Merge Intervals</h2>\n<p>Merge all overlapping intervals. <strong>Sort by start</strong>, then sweep: extend the last merged interval's end when the next one overlaps, otherwise start a new interval. Sorting exposes the greedy sweep. <strong>O(n log n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def merge(intervals: list[list[int]]) -&gt; list[list[int]]:\n    intervals.sort(key=lambda x: x[0])\n    merged = []\n    for start, end in intervals:\n        if merged and start &lt;= merged[-1][1]:\n            merged[-1][1] = max(merged[-1][1], end)   # overlap → extend\n        else:\n            merged.append([start, end])\n    return merged</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nstd::vector&lt;std::vector&lt;int&gt;&gt; merge(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; intervals) {\n    std::sort(intervals.begin(), intervals.end());\n    std::vector&lt;std::vector&lt;int&gt;&gt; merged;\n    for (auto&amp; iv : intervals) {\n        if (!merged.empty() &amp;&amp; iv[0] &lt;= merged.back()[1])\n            merged.back()[1] = std::max(merged.back()[1], iv[1]);\n        else\n            merged.push_back(iv);\n    }\n    return merged;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"4-non-overlapping-intervals\">4. Non-overlapping Intervals</h2>\n<p>Minimum intervals to remove so the rest don't overlap. <strong>Sort by end time</strong> and greedily keep each interval whose start is ≥ the last kept end (classic interval scheduling); remove the rest. Earliest-finish-first is the provably safe greedy. <strong>O(n log n)</strong> time.</p>\n<div class=\"sol\"><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def erase_overlap_intervals(intervals: list[list[int]]) -&gt; int:\n    intervals.sort(key=lambda x: x[1])      # earliest finish first\n    removed, last_end = 0, float(\"-inf\")\n    for start, end in intervals:\n        if start &gt;= last_end:               # compatible → keep it\n            last_end = end\n        else:\n            removed += 1                     # overlaps → drop it\n    return removed</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n#include &lt;climits&gt;\n\nint eraseOverlapIntervals(std::vector&lt;std::vector&lt;int&gt;&gt;&amp; intervals) {\n    std::sort(intervals.begin(), intervals.end(),\n              [](const auto&amp; a, const auto&amp; b) { return a[1] &lt; b[1]; });\n    int removed = 0; long lastEnd = LONG_MIN;\n    for (auto&amp; iv : intervals) {\n        if (iv[0] &gt;= lastEnd) lastEnd = iv[1];\n        else removed++;\n    }\n    return removed;\n}</code></pre></div></div>\n<hr>\n<details class=\"qa\"><summary>How do you justify a greedy algorithm in an interview?</summary><div class=\"qa-body\"><p>With an <strong>exchange argument</strong>: assume an optimal solution differs from the greedy one, then show you can swap the first differing choice for the greedy choice <strong>without making the solution worse</strong> — so a greedy-consistent optimum exists. For interval scheduling, picking the earliest finish time leaves the most room for later intervals, so any optimal schedule can be rewritten to start with the greedy pick. If you can't build that argument, the problem probably needs DP.</p></div></details>\n<details class=\"qa\"><summary>Why sort by <em>end</em> for interval scheduling but by <em>start</em> for merging?</summary><div class=\"qa-body\"><p>They optimize different things. <strong>Merging</strong> cares about adjacency along the timeline, so sorting by <strong>start</strong> lets a single left-to-right sweep coalesce overlaps. <strong>Max non-overlapping scheduling</strong> wants to free up future capacity as early as possible, so sorting by <strong>end</strong> and always taking the earliest-finishing compatible interval is the safe greedy — it dominates any alternative first choice.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "How it works",
        "id": "how-it-works"
      },
      {
        "level": 2,
        "txt": "1. Gas Station",
        "id": "1-gas-station"
      },
      {
        "level": 2,
        "txt": "2. Jump Game II",
        "id": "2-jump-game-ii"
      },
      {
        "level": 2,
        "txt": "3. Merge Intervals",
        "id": "3-merge-intervals"
      },
      {
        "level": 2,
        "txt": "4. Non-overlapping Intervals",
        "id": "4-non-overlapping-intervals"
      }
    ]
  }
];
