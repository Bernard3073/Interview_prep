/* AUTO-GENERATED from study-materials/data-structure-topics/*.md by build_ds_topics.cjs.
   Edit the source there and rebuild: node build_ds_topics.cjs */
const DS_TOPICS = [
  {
    "slug": "arrays-hashing",
    "title": "Arrays & Hashing — the O(1)-lookup workhorse",
    "html": "<blockquote class=\"lec-callout\">The two structures you use in almost every problem. An <strong>array</strong> is contiguous memory with O(1) random access; a <strong>hash table</strong> trades ordering for O(1)-average lookup by key. Most \"make it faster than O(n²)\" moments are really \"add a hash map.\"</blockquote>\n<hr>\n<h2 id=\"the-array\">The array</h2>\n<p>A block of equal-sized slots laid out contiguously in memory. Index <code>i</code> lives at <code>base + i × elem_size</code>, so <strong>access by index is O(1)</strong> and iteration is cache-friendly (the CPU prefetches the next slots). The cost lives at the edges:</p>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Cost</th><th>Why</th></tr></thead><tbody><tr><td>Read/write <code>a[i]</code></td><td>O(1)</td><td>direct address arithmetic</td></tr><tr><td>Append (dynamic array)</td><td>O(1) amortized</td><td>doubles capacity, occasional O(n) copy</td></tr><tr><td>Insert / delete at middle</td><td>O(n)</td><td>shift every element after the gap</td></tr><tr><td>Search (unsorted)</td><td>O(n)</td><td>must scan</td></tr><tr><td>Search (sorted)</td><td>O(log n)</td><td>binary search</td></tr></tbody></table></div>\n<p>A <strong>dynamic array</strong> (<code>list</code>, <code>std::vector</code>) hides growth: when full it allocates a bigger block (usually ×2) and copies. Because doublings are rare, the <em>amortized</em> append cost is O(1) even though a single append can be O(n).</p>\n<blockquote class=\"lec-callout\"><strong>C++ tip:</strong> call <code>vec.reserve(n)</code> when you know the final size — it skips the reallocation/copy churn. <strong>Python tip:</strong> a list comprehension beats repeated <code>.append()</code> in a loop.</blockquote>\n<hr>\n<h2 id=\"the-hash-table\">The hash table</h2>\n<p>A hash table stores key→value by computing <code>index = hash(key) % capacity</code> and dropping the entry in that bucket. A good hash spreads keys uniformly, so lookup/insert/delete are <strong>O(1) average</strong>. Two keys can land in the same bucket — a <strong>collision</strong> — resolved by chaining (a list per bucket) or open addressing (probe for the next free slot). When the <strong>load factor</strong> (entries ÷ buckets) gets near 1, the table <strong>rehashes</strong> into a larger array.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-hashmap.svg\" alt=\"Hash map: key hashed to a bucket, with a collision chain\" loading=\"lazy\"></p>\n<ul><li><strong>Average</strong> O(1); <strong>worst case</strong> O(n) if every key collides (adversarial input or a bad hash).</li><li><strong>No ordering</strong> — iteration order is arbitrary (Python <code>dict</code> preserves <em>insertion</em> order as an implementation detail; <code>unordered_map</code> gives no guarantee). Need sorted keys? That's a balanced BST (<code>std::map</code>), not a hash table.</li><li>Keys must be <strong>hashable / immutable</strong> (a tuple, not a list).</li></ul>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Need</th><th>Python</th><th>C++</th></tr></thead><tbody><tr><td>key → value</td><td><code>dict</code></td><td><code>std::unordered_map</code></td></tr><tr><td>membership / dedup</td><td><code>set</code></td><td><code>std::unordered_set</code></td></tr><tr><td>counting</td><td><code>collections.Counter</code></td><td><code>unordered_map&lt;T,int&gt;</code></td></tr><tr><td>group by key</td><td><code>collections.defaultdict(list)</code></td><td><code>unordered_map&lt;K, vector&lt;V&gt;&gt;</code></td></tr></tbody></table></div>\n<hr>\n<h2 id=\"the-core-move-turn-o-n-into-o-n\">The core move: turn O(n²) into O(n)</h2>\n<p>Whenever you catch a nested loop asking <em>\"is there another element such that…?\"</em>, a hash map usually collapses it: remember what you've seen, then each element does an O(1) lookup instead of an O(n) rescan. Two Sum is the archetype.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>seen = empty map            # value -&gt; index\nfor i, x in enumerate(nums):\n    need = target - x\n    if need in seen:        # O(1)\n        return [seen[need], i]\n    seen[x] = i</code></pre>\n<div class=\"sol\"><div class=\"sol-title\">Two Sum — one pass, O(n) time / O(n) space</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def two_sum(nums, target):\n    seen = {}                       # value -&gt; index\n    for i, x in enumerate(nums):\n        if target - x in seen:      # complement already stored?\n            return [seen[target - x], i]\n        seen[x] = i\n    return []</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) {\n    unordered_map&lt;int,int&gt; seen;            // value -&gt; index\n    for (int i = 0; i &lt; (int)nums.size(); ++i) {\n        auto it = seen.find(target - nums[i]);\n        if (it != seen.end()) return {it-&gt;second, i};\n        seen[nums[i]] = i;\n    }\n    return {};\n}</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Group Anagrams — hash by a canonical key</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import defaultdict\n\ndef group_anagrams(words):\n    groups = defaultdict(list)\n    for w in words:\n        key = tuple(sorted(w))      # same letters -&gt; same key\n        groups[key].append(w)\n    return list(groups.values())</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>vector&lt;vector&lt;string&gt;&gt; groupAnagrams(vector&lt;string&gt;&amp; words) {\n    unordered_map&lt;string, vector&lt;string&gt;&gt; groups;\n    for (auto&amp; w : words) {\n        string key = w;\n        sort(key.begin(), key.end());   // canonical form\n        groups[key].push_back(w);\n    }\n    vector&lt;vector&lt;string&gt;&gt; out;\n    for (auto&amp; [k, v] : groups) out.push_back(move(v));\n    return out;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"prefix-sums-the-other-array-superpower\">Prefix sums — the other array superpower</h2>\n<p>Precompute <code>pre[i] = a[0] + … + a[i-1]</code> once (O(n)); then <strong>any</strong> range sum <code>a[l..r]</code> is <code>pre[r+1] - pre[l]</code> in O(1). Pairing prefix sums with a hash map counts subarrays with a target sum in one pass.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>pre = 0; count = 0; seen = {0: 1}       # prefix-sum -&gt; how many times seen\nfor x in nums:\n    pre += x\n    count += seen.get(pre - k, 0)       # a previous prefix makes a k-sum window\n    seen[pre] = seen.get(pre, 0) + 1</code></pre>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>\"Have I seen…?\" / \"does a pair/complement exist?\"</strong> → hash set/map, one pass.</li><li><strong>Repeated range-sum queries on a static array</strong> → prefix sums.</li><li><strong>Counting / frequencies / grouping</strong> → <code>Counter</code> / <code>defaultdict</code>.</li><li>State the <strong>average vs worst case</strong> — O(1) hashing degrades to O(n) with collisions.</li><li>Watch mutation-while-iterating and <strong>unhashable keys</strong> (convert lists to tuples).</li></ol>\n<details class=\"qa\"><summary>Why is a hash map \"O(1) average\" but not just \"O(1)\"?</summary><div class=\"qa-body\"><p>The average assumes a good hash spreading keys across buckets with a bounded load factor, so each bucket holds ~O(1) entries. Adversarial keys (or a weak hash) can pile everything into one bucket, degrading lookups to O(n). Rehashing keeps the load factor low, and a single insert that triggers a rehash is O(n) — amortized O(1) across many inserts.</p></div></details>\n<details class=\"qa\"><summary>When would you deliberately avoid a hash map?</summary><div class=\"qa-body\"><p>When you need <strong>ordering</strong> (sorted iteration, successor/floor queries → balanced BST), when keys are a <strong>small dense integer range</strong> (a plain array is faster and simpler), when <strong>worst-case guarantees</strong> matter (a balanced BST is O(log n) worst case vs a hash map's O(n)), or in <strong>memory-tight / cache-sensitive</strong> hot loops where contiguous arrays win.</p></div></details>\n<details class=\"qa\"><summary>A <code>set</code> and a <code>dict</code> both give O(1) lookup — when do you pick which?</summary><div class=\"qa-body\"><p>A <code>set</code> stores keys only (membership, dedup, \"seen?\"). A <code>dict</code> maps keys to values (counts, indices, adjacency). If you find yourself storing <code>True</code> as every value in a dict, you want a set.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "The array",
        "id": "the-array"
      },
      {
        "level": 2,
        "txt": "The hash table",
        "id": "the-hash-table"
      },
      {
        "level": 2,
        "txt": "The core move: turn O(n²) into O(n)",
        "id": "the-core-move-turn-o-n-into-o-n"
      },
      {
        "level": 2,
        "txt": "Prefix sums — the other array superpower",
        "id": "prefix-sums-the-other-array-superpower"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "stacks-queues",
    "title": "Stacks & Queues — order-restricted access",
    "html": "<blockquote class=\"lec-callout\">Both are sequences that restrict <em>where</em> you add and remove — and that restriction is exactly what makes them useful. A <strong>stack</strong> is LIFO (last in, first out); a <strong>queue</strong> is FIFO (first in, first out). A <strong>deque</strong> allows both ends. All operations are O(1).</blockquote>\n<hr>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-stack-queue.svg\" alt=\"Stack (LIFO) vs queue (FIFO), with the allowed ends highlighted\" loading=\"lazy\"></p>\n<h2 id=\"stack-lifo\">Stack (LIFO)</h2>\n<p>Push and pop happen at the <strong>same end</strong> (the \"top\"). Think of a stack of plates. It's the natural model for anything with <strong>nesting or \"most recent first\"</strong> semantics: the function call stack, undo history, depth-first search, matching brackets, evaluating expressions.</p>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Cost</th></tr></thead><tbody><tr><td>push / pop / peek</td><td>O(1)</td></tr><tr><td>search</td><td>O(n)</td></tr></tbody></table></div>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>push(x):  arr.append(x)\npop():    return arr.pop()          # removes &amp; returns the top\npeek():   return arr[-1]            # look without removing</code></pre>\n<p><strong>Implementations:</strong> Python — a plain <code>list</code> (<code>append</code> / <code>pop</code>). C++ — <code>std::stack</code> (or a <code>vector</code>). You rarely need a linked list for this.</p>\n<h3 id=\"the-monotonic-stack\">The monotonic stack</h3>\n<p>A stack you keep <strong>sorted</strong> (increasing or decreasing) by popping violators before you push. It answers <em>\"next greater / previous smaller element\"</em> for every position in <strong>O(n) total</strong> — each element is pushed and popped at most once.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code># next greater element to the right, for each index\nstack = []                          # holds indices, values decreasing\nfor i in range(n):\n    while stack and nums[stack[-1]] &lt; nums[i]:\n        j = stack.pop()\n        answer[j] = nums[i]         # nums[i] is j's next-greater\n    stack.append(i)</code></pre>\n<div class=\"sol\"><div class=\"sol-title\">Valid Parentheses — the canonical stack</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def is_valid(s: str) -&gt; bool:\n    pairs = {')': '(', ']': '[', '}': '{'}\n    stack = []\n    for c in s:\n        if c in pairs:                          # closing bracket\n            if not stack or stack.pop() != pairs[c]:\n                return False\n        else:\n            stack.append(c)                     # opening bracket\n    return not stack                            # nothing left unmatched</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>bool isValid(const string&amp; s) {\n    unordered_map&lt;char,char&gt; pairs{{')','('},{']','['},{'}','{'}};\n    stack&lt;char&gt; st;\n    for (char c : s) {\n        if (pairs.count(c)) {\n            if (st.empty() || st.top() != pairs[c]) return false;\n            st.pop();\n        } else st.push(c);\n    }\n    return st.empty();\n}</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Daily Temperatures — monotonic stack, O(n)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def daily_temperatures(temps):\n    ans = [0] * len(temps)\n    stack = []                      # indices, temps decreasing\n    for i, t in enumerate(temps):\n        while stack and temps[stack[-1]] &lt; t:\n            j = stack.pop()\n            ans[j] = i - j          # days until a warmer temp\n        stack.append(i)\n    return ans</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>vector&lt;int&gt; dailyTemperatures(vector&lt;int&gt;&amp; temps) {\n    int n = temps.size();\n    vector&lt;int&gt; ans(n, 0);\n    stack&lt;int&gt; st;                  // indices, temps decreasing\n    for (int i = 0; i &lt; n; ++i) {\n        while (!st.empty() &amp;&amp; temps[st.top()] &lt; temps[i]) {\n            int j = st.top(); st.pop();\n            ans[j] = i - j;\n        }\n        st.push(i);\n    }\n    return ans;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"queue-fifo\">Queue (FIFO)</h2>\n<p>Add at the <strong>back</strong>, remove from the <strong>front</strong>. The model for <strong>fair, in-order processing</strong>: the BFS frontier, task/print scheduling, buffering a producer→consumer stream.</p>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Cost</th></tr></thead><tbody><tr><td>enqueue (push back)</td><td>O(1)</td></tr><tr><td>dequeue (pop front)</td><td>O(1)</td></tr><tr><td>peek front</td><td>O(1)</td></tr></tbody></table></div>\n<blockquote class=\"lec-callout\"><strong>Critical gotcha:</strong> in Python, do <strong>not</strong> use <code>list.pop(0)</code> as a dequeue — it shifts every remaining element and is <strong>O(n)</strong>. Use <code>collections.deque</code> (<code>append</code> / <code>popleft</code>, both O(1)). In C++, <code>std::queue</code>.</blockquote>\n<h3 id=\"deque-a-double-ended-queue\">Deque — a double-ended queue</h3>\n<p>Push/pop at <strong>both</strong> ends in O(1). It generalizes both structures and powers the <strong>sliding-window maximum</strong> (keep a decreasing deque of candidate indices) and fixed-size <strong>ring buffers</strong>.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code># sliding-window maximum: deque holds indices, values decreasing\ndq = deque()\nfor i in range(n):\n    while dq and nums[dq[-1]] &lt;= nums[i]:   # pop smaller from the back\n        dq.pop()\n    dq.append(i)\n    if dq[0] &lt;= i - k:                      # drop index that left the window\n        dq.popleft()\n    if i &gt;= k - 1:\n        output.append(nums[dq[0]])          # front = window max</code></pre>\n<div class=\"sol\"><div class=\"sol-title\">Implement a Queue using two Stacks — amortized O(1)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class MyQueue:\n    def __init__(self):\n        self.inb, self.outb = [], []        # in-box, out-box\n\n    def push(self, x):\n        self.inb.append(x)\n\n    def _shift(self):\n        if not self.outb:                   # reverse in -&gt; out, once\n            while self.inb:\n                self.outb.append(self.inb.pop())\n\n    def pop(self):\n        self._shift()\n        return self.outb.pop()\n\n    def peek(self):\n        self._shift()\n        return self.outb[-1]\n\n    def empty(self):\n        return not self.inb and not self.outb</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>class MyQueue {\n    stack&lt;int&gt; inb, outb;\n    void shift() {\n        if (outb.empty())\n            while (!inb.empty()) { outb.push(inb.top()); inb.pop(); }\n    }\npublic:\n    void push(int x) { inb.push(x); }\n    int pop()  { shift(); int v = outb.top(); outb.pop(); return v; }\n    int peek() { shift(); return outb.top(); }\n    bool empty() { return inb.empty() &amp;&amp; outb.empty(); }\n};</code></pre></div></div>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>Nesting / matching / \"most recent\" / undo / DFS</strong> → stack.</li><li><strong>\"Next greater / smaller\", histogram, span</strong> → monotonic stack, O(n).</li><li><strong>In-order / level-by-level / BFS / buffering</strong> → queue.</li><li><strong>Window extremes, both-ends access</strong> → deque.</li><li>Say **\"deque, not <code>list.pop(0)</code>\"** out loud — it's the classic Python complexity trap.</li></ol>\n<details class=\"qa\"><summary>Why is each element in a monotonic-stack pass amortized O(1) despite the inner <code>while</code>?</summary><div class=\"qa-body\"><p>Every element is <strong>pushed exactly once and popped at most once</strong> over the whole scan. The inner <code>while</code> can pop several elements in one step, but those pops are \"paid for\" by earlier pushes — the total number of push+pop operations is ≤ 2n, so the whole pass is O(n), not O(n²).</p></div></details>\n<details class=\"qa\"><summary>Implement a stack using queues, or a queue using stacks — what's the trick?</summary><div class=\"qa-body\"><p><strong>Queue from two stacks:</strong> push to an in-box; to pop, if the out-box is empty, pour the in-box into it (reversing order), then pop the out-box — amortized O(1). <strong>Stack from one queue:</strong> after each push, rotate the queue so the new element sits at the front (O(n) push, O(1) pop). Know which operation you made expensive.</p></div></details>\n<details class=\"qa\"><summary>When do you need an actual linked-list-backed queue instead of an array deque?</summary><div class=\"qa-body\"><p>Almost never in interviews — a <code>deque</code> (ring buffer / doubly linked list under the hood) already gives O(1) both ends. Real systems use a linked or chunked queue mainly for <strong>unbounded growth without reallocation</strong> or <strong>lock-free concurrent</strong> access.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "Stack (LIFO)",
        "id": "stack-lifo"
      },
      {
        "level": 3,
        "txt": "The monotonic stack",
        "id": "the-monotonic-stack"
      },
      {
        "level": 2,
        "txt": "Queue (FIFO)",
        "id": "queue-fifo"
      },
      {
        "level": 3,
        "txt": "Deque — a double-ended queue",
        "id": "deque-a-double-ended-queue"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "heaps-priority-queues",
    "title": "Heaps & Priority Queues — always pull the extreme next",
    "html": "<blockquote class=\"lec-callout\">A <strong>priority queue</strong> hands you the smallest (or largest) element next, no matter the insertion order. The usual implementation is a <strong>binary heap</strong>: a complete binary tree stored in a plain array, with O(log n) insert/remove and O(1) peek.</blockquote>\n<hr>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-heap.svg\" alt=\"Min-heap as a tree and its array layout, plus the top-k trick\" loading=\"lazy\"></p>\n<h2 id=\"the-binary-heap\">The binary heap</h2>\n<p>A <strong>min-heap</strong> maintains one invariant: every parent is <strong>≤ both children</strong>. That's weaker than a sorted array — siblings are unordered — which is why it's cheap to maintain, yet it guarantees the <strong>minimum is always at the root</strong>. The tree is <em>complete</em> (filled left to right), so it packs into an array with no gaps:</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>node at index i:\n    parent(i)      = (i - 1) // 2\n    left(i)        = 2*i + 1\n    right(i)       = 2*i + 2</code></pre>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Cost</th><th>How</th></tr></thead><tbody><tr><td>peek min</td><td>O(1)</td><td>it's <code>heap[0]</code></td></tr><tr><td>push</td><td>O(log n)</td><td>append, then <strong>sift up</strong> while smaller than parent</td></tr><tr><td>pop min</td><td>O(log n)</td><td>swap root with last, remove, <strong>sift down</strong></td></tr><tr><td>build from n items</td><td><strong>O(n)</strong></td><td><code>heapify</code> — cheaper than n pushes</td></tr><tr><td>search / arbitrary delete</td><td>O(n)</td><td>heap is <em>not</em> sorted</td></tr></tbody></table></div>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>push(x):\n    heap.append(x)\n    i = len(heap) - 1\n    while i &gt; 0 and heap[parent(i)] &gt; heap[i]:\n        swap(i, parent(i)); i = parent(i)          # sift up\n\npop():\n    swap(0, last); m = heap.pop()                  # remove old root\n    i = 0\n    while True:                                    # sift down\n        s = smallest of (i, left(i), right(i))\n        if s == i: break\n        swap(i, s); i = s\n    return m</code></pre>\n<blockquote class=\"lec-callout\"><strong>Build is O(n), not O(n log n).</strong> Sifting down from the bottom up, most nodes are shallow, and the sum ∑ (height work) telescopes to O(n). Prefer <code>heapify(list)</code> over pushing one at a time.</blockquote>\n<hr>\n<h2 id=\"library-types-mind-the-default-direction\">Library types (mind the default direction)</h2>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th></th><th>Python <code>heapq</code></th><th>C++ <code>std::priority_queue</code></th></tr></thead><tbody><tr><td>Default</td><td><strong>min</strong>-heap</td><td><strong>max</strong>-heap</td></tr><tr><td>Push / pop</td><td><code>heappush</code>, <code>heappop</code></td><td><code>push</code>, <code>pop</code> (top = max)</td></tr><tr><td>Flip direction</td><td>push <strong>negated</strong> values, or <code>(-key, item)</code></td><td><code>priority_queue&lt;int, vector&lt;int&gt;, greater&lt;&gt;&gt;</code></td></tr><tr><td>Build in O(n)</td><td><code>heapify(list)</code></td><td>construct from iterators</td></tr></tbody></table></div>\n<p>Store a <strong>tuple</strong> <code>(priority, tiebreak, payload)</code> to sort by a key; make sure the tiebreak is comparable so Python never tries to compare unorderable payloads.</p>\n<hr>\n<h2 id=\"what-heaps-are-for\">What heaps are <em>for</em></h2>\n<ul><li><strong>Top-k / k-th largest</strong> — keep a size-k heap: O(n log k) time, O(k) memory (below).</li><li><strong>Streaming median</strong> — a max-heap of the low half + a min-heap of the high half.</li><li><strong>Merge k sorted lists / streams</strong> — a heap of the k current heads.</li><li><strong>Dijkstra / Prim</strong> — always expand the closest frontier node.</li><li><strong>Scheduling</strong> — run the highest-priority / earliest-deadline task next.</li></ul>\n<div class=\"sol\"><div class=\"sol-title\">K-th largest — size-k min-heap, O(n log k)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import heapq\n\ndef kth_largest(nums, k):\n    heap = nums[:k]\n    heapq.heapify(heap)                 # O(k)\n    for x in nums[k:]:\n        if x &gt; heap[0]:                 # bigger than the current k-th largest?\n            heapq.heapreplace(heap, x)  # pop min, push x — O(log k)\n    return heap[0]                      # root = k-th largest</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>int kthLargest(vector&lt;int&gt;&amp; nums, int k) {\n    priority_queue&lt;int, vector&lt;int&gt;, greater&lt;int&gt;&gt; heap;   // min-heap, size k\n    for (int x : nums) {\n        heap.push(x);\n        if ((int)heap.size() &gt; k) heap.pop();              // drop the smallest\n    }\n    return heap.top();\n}</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Streaming median — two heaps</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>import heapq\n\nclass MedianFinder:\n    def __init__(self):\n        self.lo = []    # max-heap (store negatives): smaller half\n        self.hi = []    # min-heap: larger half\n\n    def add(self, x):\n        heapq.heappush(self.lo, -x)\n        heapq.heappush(self.hi, -heapq.heappop(self.lo))    # balance top across\n        if len(self.hi) &gt; len(self.lo):                     # keep lo &gt;= hi in size\n            heapq.heappush(self.lo, -heapq.heappop(self.hi))\n\n    def median(self):\n        if len(self.lo) &gt; len(self.hi):\n            return -self.lo[0]\n        return (-self.lo[0] + self.hi[0]) / 2</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>class MedianFinder {\n    priority_queue&lt;int&gt; lo;                                  // max-heap: lower half\n    priority_queue&lt;int, vector&lt;int&gt;, greater&lt;int&gt;&gt; hi;       // min-heap: upper half\npublic:\n    void add(int x) {\n        lo.push(x);\n        hi.push(lo.top()); lo.pop();                         // balance across\n        if (hi.size() &gt; lo.size()) { lo.push(hi.top()); hi.pop(); }\n    }\n    double median() {\n        if (lo.size() &gt; hi.size()) return lo.top();\n        return (lo.top() + hi.top()) / 2.0;\n    }\n};</code></pre></div></div>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>\"k largest / smallest / closest / most frequent\"</strong> → size-k heap, O(n log k).</li><li><strong>\"Running / streaming median\"</strong> → two heaps, balanced by size.</li><li><strong>\"Merge k sorted …\"</strong> → heap of k heads.</li><li>State the <strong>direction gotcha</strong>: <code>heapq</code> is min, <code>priority_queue</code> is max — negate to flip.</li><li>Build from a batch with **<code>heapify</code> (O(n))**, not n pushes.</li></ol>\n<details class=\"qa\"><summary>Top-k with a heap is O(n log k). When is sorting (O(n log n)) or quickselect (O(n)) better?</summary><div class=\"qa-body\"><p>Sort when you also need the elements <strong>in order</strong>, or k is close to n. <strong>Quickselect</strong> finds the k-th element (and partitions around it) in <strong>O(n) average</strong> with O(1) extra space — best when you want the k-th value or an unordered top-k in one shot and can tolerate O(n²) worst case. The heap wins for <strong>streaming</strong> (you can't sort data you haven't seen) and small k with bounded memory.</p></div></details>\n<details class=\"qa\"><summary>Why is <code>heapify</code> O(n) when it looks like n sift-downs of O(log n) each?</summary><div class=\"qa-body\"><p>Sift-down cost is proportional to a node's <strong>height</strong>, and most nodes are near the leaves with tiny height. Summing height over all nodes, ∑ h · (nodes at height h), telescopes to O(n) — the many shallow nodes dominate the count, and the few tall ones are rare. Building by repeated <strong>push</strong> (sift-up) really is O(n log n) because the deep leaves are the majority and sift-up cost grows with depth.</p></div></details>\n<details class=\"qa\"><summary>How do you delete or update an arbitrary element in a heap (e.g. Dijkstra's decrease-key)?</summary><div class=\"qa-body\"><p>A binary heap can't find an arbitrary element in O(log n). Two standard fixes: <strong>lazy deletion</strong> — push the updated entry and skip stale ones when popped (mark-and-ignore); or keep a <strong>position map</strong> (value → index) and sift after an in-place change. Interviews almost always accept lazy deletion.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "The binary heap",
        "id": "the-binary-heap"
      },
      {
        "level": 2,
        "txt": "Library types (mind the default direction)",
        "id": "library-types-mind-the-default-direction"
      },
      {
        "level": 2,
        "txt": "What heaps are *for*",
        "id": "what-heaps-are-for"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "linked-lists",
    "title": "Linked Lists — pointers instead of contiguity",
    "html": "<blockquote class=\"lec-callout\">A chain of nodes, each holding a value and a pointer to the next. No contiguous block, so <strong>no O(1) random access</strong> — but <strong>O(1) insert/delete once you hold the node</strong>, with no shifting. The interview value is less the structure itself and more the <strong>pointer-manipulation reflexes</strong> it drills.</blockquote>\n<hr>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-linkedlist.svg\" alt=\"Singly and doubly linked lists; O(1) middle removal by relinking\" loading=\"lazy\"></p>\n<h2 id=\"shapes\">Shapes</h2>\n<ul><li><strong>Singly linked:</strong> each node has <code>next</code>. Traverse forward only.</li><li><strong>Doubly linked:</strong> each node has <code>next</code> <strong>and</strong> <code>prev</code>. Traverse both ways; remove a node in O(1) given only that node (relink its neighbors). Backs the <strong>LRU cache</strong> and <code>std::list</code>.</li><li><strong>Circular:</strong> the tail's <code>next</code> points back to the head — round-robin schedulers, ring buffers.</li></ul>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Array</th><th>Linked list</th></tr></thead><tbody><tr><td>Access index i</td><td>O(1)</td><td>O(n) (walk from head)</td></tr><tr><td>Insert/delete at a <strong>held</strong> node</td><td>O(n) (shift)</td><td><strong>O(1)</strong> (relink)</td></tr><tr><td>Insert/delete at head</td><td>O(n)</td><td>O(1)</td></tr><tr><td>Search by value</td><td>O(n)</td><td>O(n)</td></tr><tr><td>Memory / cache</td><td>contiguous, friendly</td><td>scattered, pointer-chasing</td></tr></tbody></table></div>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>class Node:\n    val\n    next        # and prev, for a doubly linked list</code></pre>\n<blockquote class=\"lec-callout\"><strong>Reality check:</strong> arrays usually beat linked lists in practice because contiguous memory is cache-friendly while pointer-chasing thrashes the cache. Reach for a linked list when you need <strong>O(1) splice/erase at a node you already hold</strong> (LRU eviction, free lists) — not for generic sequences.</blockquote>\n<hr>\n<h2 id=\"the-two-reflexes-dummy-head-fast-slow-pointers\">The two reflexes: dummy head &amp; fast/slow pointers</h2>\n<p><strong>Dummy (sentinel) head</strong> — allocate a throwaway node before the real head so insertion/deletion at the front needs no special case; return <code>dummy.next</code> at the end.</p>\n<p><strong>Fast/slow pointers (Floyd)</strong> — advance one pointer by 1 and another by 2. When fast reaches the end, slow is at the <strong>middle</strong>; if there's a <strong>cycle</strong>, they eventually meet.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code># detect a cycle\nslow = head; fast = head\nwhile fast and fast.next:\n    slow = slow.next\n    fast = fast.next.next\n    if slow is fast:\n        return True          # pointers met -&gt; cycle\nreturn False</code></pre>\n<div class=\"sol\"><div class=\"sol-title\">Reverse a singly linked list — iterative, O(n) / O(1)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def reverse_list(head):\n    prev = None\n    while head:\n        nxt = head.next     # save next\n        head.next = prev    # flip the pointer\n        prev = head         # advance prev\n        head = nxt          # advance head\n    return prev             # new head</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>ListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    while (head) {\n        ListNode* nxt = head-&gt;next;   // save\n        head-&gt;next = prev;            // flip\n        prev = head;                  // advance\n        head = nxt;\n    }\n    return prev;\n}</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Merge two sorted lists — dummy head</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def merge(a, b):\n    dummy = tail = ListNode()           # sentinel avoids head special-case\n    while a and b:\n        if a.val &lt;= b.val:\n            tail.next, a = a, a.next\n        else:\n            tail.next, b = b, b.next\n        tail = tail.next\n    tail.next = a or b                  # attach the remainder\n    return dummy.next</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>ListNode* merge(ListNode* a, ListNode* b) {\n    ListNode dummy, *tail = &amp;dummy;\n    while (a &amp;&amp; b) {\n        if (a-&gt;val &lt;= b-&gt;val) { tail-&gt;next = a; a = a-&gt;next; }\n        else                  { tail-&gt;next = b; b = b-&gt;next; }\n        tail = tail-&gt;next;\n    }\n    tail-&gt;next = a ? a : b;\n    return dummy.next;\n}</code></pre></div></div>\n<hr>\n<h2 id=\"why-the-lru-cache-pairs-a-hash-map-with-a-doubly-linked-list\">Why the LRU cache pairs a hash map with a doubly linked list</h2>\n<p>An LRU cache needs two O(1) operations: <strong>find a key</strong> and <strong>move it to \"most recently used.\"</strong> A hash map gives O(1) find (key → node); a doubly linked list gives O(1) move/evict (unlink a node, splice it to the front, drop the tail). Neither alone suffices — together they hit O(1) for both <code>get</code> and <code>put</code>.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>get(key):   node = map[key]; move node to front; return node.val\nput(key,v): if key in map: update, move to front\n            else: insert at front; map[key]=node\n                  if size &gt; capacity: evict tail; delete map[tail.key]</code></pre>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>Reversal / reordering / merging</strong> → track <code>prev</code>/<code>next</code> carefully; draw the pointers.</li><li><strong>Middle, cycle, k-th from end</strong> → fast/slow pointers.</li><li><strong>Head might change</strong> → dummy sentinel node.</li><li><strong>O(1) get + O(1) recency update</strong> → hash map + doubly linked list (LRU).</li><li>In C++ mind <strong>ownership</strong> — who frees the nodes (raw pointers vs <code>unique_ptr</code>).</li></ol>\n<details class=\"qa\"><summary>What's the single most useful trick for linked-list problems, and why?</summary><div class=\"qa-body\"><p>The <strong>dummy head</strong>. Front insertions/deletions and \"the head itself might be removed\" are the bug-prone cases; a sentinel node before the head makes every position uniform, so you write one code path and return <code>dummy.next</code>. It eliminates most null-pointer edge cases.</p></div></details>\n<details class=\"qa\"><summary>Find the middle, detect a cycle, and find the cycle's start — all with two pointers?</summary><div class=\"qa-body\"><p><strong>Middle:</strong> slow +1, fast +2; when fast ends, slow is the middle. <strong>Cycle detection:</strong> same walk — if they ever meet, there's a cycle (Floyd). <strong>Cycle start:</strong> after they meet, reset one pointer to the head and advance both by 1; they meet at the entry node (a distance argument shows why).</p></div></details>\n<details class=\"qa\"><summary>Doubly vs singly linked — what does the extra <code>prev</code> pointer buy, and what does it cost?</summary><div class=\"qa-body\"><p><code>prev</code> lets you <strong>delete a node in O(1) given only that node</strong> (relink both neighbors) and traverse backward — essential for LRU and <code>std::list</code>. The cost is an extra pointer per node (more memory) and two links to maintain on every insert/delete, so it's easier to introduce bugs.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "Shapes",
        "id": "shapes"
      },
      {
        "level": 2,
        "txt": "The two reflexes: dummy head & fast/slow pointers",
        "id": "the-two-reflexes-dummy-head-fast-slow-pointers"
      },
      {
        "level": 2,
        "txt": "Why the LRU cache pairs a hash map with a doubly linked list",
        "id": "why-the-lru-cache-pairs-a-hash-map-with-a-doubly-linked-list"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "trees",
    "title": "Trees — the whole family, not just the BST",
    "html": "<blockquote class=\"lec-callout\">A <strong>tree</strong> is a connected, acyclic graph with a single <strong>root</strong>: every node has exactly one parent (except the root) and any number of children. It's the shape of hierarchy — file systems, the DOM, parse trees, decision trees — and the substrate for a whole family of specialized structures (BSTs, heaps, tries, segment trees). This page introduces trees in general, then the variants you meet by name.</blockquote>\n<hr>\n<h2 id=\"vocabulary-learn-these-once\">Vocabulary (learn these once)</h2>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-tree-anatomy.svg\" alt=\"Tree anatomy — root, parent/child, leaf, depth, height, subtree\" loading=\"lazy\"></p>\n<ul><li><strong>Root</strong> — the single node with no parent; every traversal starts here.</li><li><strong>Parent / child / sibling</strong> — the obvious family relations along edges.</li><li><strong>Leaf</strong> — a node with no children; <strong>internal node</strong> — one with at least one child.</li><li><strong>Depth</strong> of a node — edges from the root down to it (root = 0).</li><li><strong>Height</strong> of the tree — the longest root-to-leaf path (= max depth).</li><li><strong>Subtree</strong> — a node together with all its descendants.</li><li><strong>Degree</strong> — number of children; a <strong>binary</strong> tree has degree ≤ 2, an <strong>N-ary</strong> tree any number.</li></ul>\n<p>Structural facts worth stating in an interview: a tree with <code>n</code> nodes has exactly <code>n − 1</code> edges, no cycles, and one root. <strong>Almost every tree operation costs O(height)</strong> — which is why <em>balance</em> (keeping height ≈ log n) is the recurring theme below.</p>\n<hr>\n<h2 id=\"the-binary-tree\">The binary tree</h2>\n<p>Each node has up to two children (<code>left</code>, <code>right</code>). Useful named forms:</p>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Form</th><th>Meaning</th></tr></thead><tbody><tr><td><strong>Full</strong></td><td>every node has 0 or 2 children</td></tr><tr><td><strong>Complete</strong></td><td>all levels full except possibly the last, filled left-to-right (this is a <strong>heap</strong>)</td></tr><tr><td><strong>Perfect</strong></td><td>all internal nodes have 2 children and all leaves at the same depth</td></tr><tr><td><strong>Balanced</strong></td><td>height is O(log n) — left/right heights differ by a bounded amount</td></tr><tr><td><strong>Degenerate</strong></td><td>a chain — height n, no better than a linked list</td></tr></tbody></table></div>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>class TreeNode:\n    val\n    left        # child subtree or null\n    right</code></pre>\n<p>A complete tree needs no pointers — it packs into an <strong>array</strong> (child of <code>i</code> at <code>2i+1</code>, <code>2i+2</code>). That's exactly how a heap is stored; see the Heaps page.</p>\n<div class=\"sol\"><div class=\"sol-title\">Building a binary tree — by hand and from a level-order list</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class TreeNode:\n    def __init__(self, val, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\n# --- By hand (bottom-up) ---\n#         1\n#        / \\\n#       2   3\n#      / \\\n#     4   5\nroot = TreeNode(1,\n        left=TreeNode(2, TreeNode(4), TreeNode(5)),\n        right=TreeNode(3))\n\n# --- From a level-order list (LeetCode style, None = missing node) ---\nfrom collections import deque\n\ndef build_tree(values):\n    if not values or values[0] is None:\n        return None\n    root = TreeNode(values[0])\n    q, i = deque([root]), 1\n    while q and i &lt; len(values):\n        node = q.popleft()\n        if i &lt; len(values) and values[i] is not None:   # left child\n            node.left = TreeNode(values[i]); q.append(node.left)\n        i += 1\n        if i &lt; len(values) and values[i] is not None:   # right child\n            node.right = TreeNode(values[i]); q.append(node.right)\n        i += 1\n    return root\n\nroot = build_tree([1, 2, 3, 4, 5, None, None])</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>#include &lt;queue&gt;\n#include &lt;vector&gt;\n#include &lt;optional&gt;\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode* left;\n    TreeNode* right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n    TreeNode(int x, TreeNode* l, TreeNode* r) : val(x), left(l), right(r) {}\n};\n\n// --- By hand (bottom-up) ---\n//         1\n//        / \\\n//       2   3\n//      / \\\n//     4   5\nTreeNode* root = new TreeNode(1,\n        new TreeNode(2, new TreeNode(4), new TreeNode(5)),\n        new TreeNode(3));\n\n// --- From a level-order list (nullopt = missing node) ---\nTreeNode* buildTree(const vector&lt;optional&lt;int&gt;&gt;&amp; values) {\n    if (values.empty() || !values[0].has_value()) return nullptr;\n    TreeNode* root = new TreeNode(values[0].value());\n    queue&lt;TreeNode*&gt; q; q.push(root);\n    size_t i = 1;\n    while (!q.empty() &amp;&amp; i &lt; values.size()) {\n        TreeNode* node = q.front(); q.pop();\n        if (i &lt; values.size() &amp;&amp; values[i].has_value()) {   // left child\n            node-&gt;left = new TreeNode(values[i].value()); q.push(node-&gt;left);\n        }\n        ++i;\n        if (i &lt; values.size() &amp;&amp; values[i].has_value()) {   // right child\n            node-&gt;right = new TreeNode(values[i].value()); q.push(node-&gt;right);\n        }\n        ++i;\n    }\n    return root;\n}\n// usage: TreeNode* root = buildTree({1, 2, 3, 4, 5, nullopt, nullopt});</code></pre></div></div>\n<hr>\n<h2 id=\"traversals-the-core-skill\">Traversals — the core skill</h2>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-tree-traversals.svg\" alt=\"Tree traversals: pre-order, in-order, post-order, and level-order on one tree\" loading=\"lazy\"></p>\n<p><strong>Depth-first (DFS)</strong> dives down before going wide — a <strong>stack</strong> (or recursion). The three orders differ only in <em>when you visit the node</em> relative to its children:</p>\n<ul><li><strong>Pre-order</strong> (node → L → R): copy/serialize a tree, prefix expressions.</li><li><strong>In-order</strong> (L → node → R): on a <strong>BST this yields sorted order</strong> — a key fact.</li><li><strong>Post-order</strong> (L → R → node): delete a tree, evaluate an expression, compute subtree aggregates (heights, sums) bottom-up.</li></ul>\n<p><strong>Breadth-first (BFS / level-order)</strong> visits level by level — a <strong>queue</strong>. Use it for shortest depth, level-by-level output, or \"closest to the root\" questions.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code># DFS (recursive)                    # BFS (iterative, level by level)\ndef dfs(node):                       q = deque([root])\n    if not node: return              while q:\n    visit(node)        # pre-order       node = q.popleft()\n    dfs(node.left)                        visit(node)\n    dfs(node.right)                       if node.left:  q.append(node.left)\n                                          if node.right: q.append(node.right)</code></pre>\n<div class=\"sol\"><div class=\"sol-title\">Level-order traversal (BFS by levels)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\ndef level_order(root):\n    if not root:\n        return []\n    out, q = [], deque([root])\n    while q:\n        level = []\n        for _ in range(len(q)):          # exactly this level's nodes\n            node = q.popleft()\n            level.append(node.val)\n            if node.left:  q.append(node.left)\n            if node.right: q.append(node.right)\n        out.append(level)\n    return out</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>vector&lt;vector&lt;int&gt;&gt; levelOrder(TreeNode* root) {\n    vector&lt;vector&lt;int&gt;&gt; out;\n    if (!root) return out;\n    queue&lt;TreeNode*&gt; q; q.push(root);\n    while (!q.empty()) {\n        int sz = q.size();\n        vector&lt;int&gt; level;\n        for (int i = 0; i &lt; sz; ++i) {       // fix the count = this level\n            TreeNode* n = q.front(); q.pop();\n            level.push_back(n-&gt;val);\n            if (n-&gt;left)  q.push(n-&gt;left);\n            if (n-&gt;right) q.push(n-&gt;right);\n        }\n        out.push_back(level);\n    }\n    return out;\n}</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Max depth (height) — post-order recursion</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def max_depth(root):\n    if not root:\n        return 0\n    return 1 + max(max_depth(root.left), max_depth(root.right))</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>int maxDepth(TreeNode* root) {\n    if (!root) return 0;\n    return 1 + max(maxDepth(root-&gt;left), maxDepth(root-&gt;right));\n}</code></pre></div></div>\n<blockquote class=\"lec-callout\"><strong>Recursion vs stack:</strong> recursion is cleaner but risks stack overflow on a degenerate tree of depth n. Convert to an explicit stack when depth can be large.</blockquote>\n<hr>\n<h2 id=\"binary-search-tree-bst\">Binary search tree (BST)</h2>\n<p>A binary tree with an <strong>ordering invariant</strong>: for every node, all keys in the left subtree are smaller and all in the right are larger. This buys <strong>O(h) search/insert/delete</strong> and — via in-order traversal — <strong>sorted iteration</strong> for free.</p>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-bst.svg\" alt=\"BST ordering invariant with in-order traversal producing sorted output\" loading=\"lazy\"></p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>search(node, key):\n    while node and node.val != key:\n        node = node.left if key &lt; node.val else node.right\n    return node</code></pre>\n<p>The catch: insert already-sorted keys into a naive BST and it degenerates into a chain (height n → O(n) ops).</p>\n<hr>\n<h2 id=\"self-balancing-trees-why-std-map-is-fast\">Self-balancing trees — why <code>std::map</code> is fast</h2>\n<p>To guarantee height O(log n) regardless of insertion order, self-balancing BSTs restructure (via <strong>rotations</strong>) after updates:</p>\n<ul><li><strong>AVL tree</strong> — strict balance (subtree heights differ by ≤ 1); faster lookups, more rotations.</li><li><strong>Red-black tree</strong> — looser balance with color rules; fewer rotations on insert/delete. This backs **<code>std::map</code> / <code>std::set</code>** and Java's <code>TreeMap</code>.</li></ul>\n<p>You won't code a red-black tree in an interview, but you should be able to say <strong>what</strong> balancing buys (worst-case O(log n)) and <strong>why</strong> you'd pick a balanced BST over a hash map: <strong>ordered</strong> operations — range queries, floor/ceiling, successor/predecessor, sorted iteration.</p>\n<blockquote class=\"lec-callout\"><strong>Python has no built-in balanced BST.</strong> Simulate ordered-structure needs with a **sorted list + <code>bisect</code>** (O(log n) search, O(n) insert), the <code>sortedcontainers</code> library, or a heap when you only need the extreme.</blockquote>\n<hr>\n<h2 id=\"trees-in-disguise\">Trees in disguise</h2>\n<ul><li><strong>Heap</strong> — a <em>complete</em> binary tree kept in an array; parent ≤ children. Pull-min in O(log n). See the Heaps page.</li><li><strong>Trie (prefix tree)</strong> — each node is a character; a root-to-node path spells a prefix. O(L) string lookup. See the Tries page.</li><li><strong>N-ary tree</strong> — arbitrary children (<code>children[]</code>); the DOM, file systems, game trees.</li><li><strong>Segment tree / Fenwick (BIT)</strong> — trees over an array index range for <strong>O(log n) range queries + point updates</strong> (range sum/min/max). Advanced, but worth naming when asked \"range queries with updates.\"</li></ul>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>Shortest depth / level-by-level / \"closest to root\"</strong> → BFS with a queue.</li><li><strong>Subtree aggregates (height, sum, diameter), delete, serialize</strong> → DFS (post/pre-order).</li><li><strong>Sorted output from a BST</strong> → in-order traversal.</li><li><strong>Ordered map/set, range/successor queries</strong> → balanced BST (<code>std::map</code>); Python: <code>bisect</code>.</li><li>State <strong>O(height)</strong> and whether the tree is <strong>balanced</strong> — it decides O(log n) vs O(n).</li></ol>\n<details class=\"qa\"><summary>What do the three DFS orders each give you, concretely?</summary><div class=\"qa-body\"><p><strong>Pre-order</strong> (node first) reproduces structure top-down — serialize/clone a tree, or emit a prefix expression. <strong>In-order</strong> (node between children) yields <strong>sorted keys on a BST</strong>, so k-th smallest = stop after k in-order visits. <strong>Post-order</strong> (node last) processes children before the parent — required for deletion, freeing memory, evaluating an expression tree, or any bottom-up aggregate like height or subtree sum.</p></div></details>\n<details class=\"qa\"><summary>Why must a BST be balanced, and what does balancing actually do?</summary><div class=\"qa-body\"><p>All BST operations cost O(height). Random or adversarial (sorted) insertions can make height grow to n, degrading everything to O(n) — a glorified linked list. <strong>Self-balancing</strong> trees (AVL, red-black) perform <strong>rotations</strong> after updates to keep height O(log n), guaranteeing worst-case O(log n) search/insert/delete. That worst-case guarantee is the main reason to choose a balanced BST over a hash map when you also need ordering.</p></div></details>\n<details class=\"qa\"><summary>BST vs hash map — when do you pick the tree?</summary><div class=\"qa-body\"><p>A hash map wins on raw point lookup (O(1) average) but is <strong>unordered</strong>. Choose a balanced BST when you need <strong>ordered</strong> operations: sorted traversal, range queries (<code>all keys in [lo, hi]</code>), floor/ceiling, successor/predecessor, or <strong>worst-case</strong> O(log n) guarantees rather than a hash map's O(n) worst case.</p></div></details>\n<details class=\"qa\"><summary>How would you serialize and deserialize a binary tree?</summary><div class=\"qa-body\"><p>Do a <strong>pre-order</strong> DFS, emitting each value and a marker (e.g. <code>#</code>) for null children — that records structure. To deserialize, consume the stream in the same pre-order: read a value, then recursively build the left subtree, then the right. Null markers tell recursion where to stop. Level-order with null markers works too.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "Vocabulary (learn these once)",
        "id": "vocabulary-learn-these-once"
      },
      {
        "level": 2,
        "txt": "The binary tree",
        "id": "the-binary-tree"
      },
      {
        "level": 2,
        "txt": "Traversals — the core skill",
        "id": "traversals-the-core-skill"
      },
      {
        "level": 2,
        "txt": "Binary search tree (BST)",
        "id": "binary-search-tree-bst"
      },
      {
        "level": 2,
        "txt": "Self-balancing trees — why `std::map` is fast",
        "id": "self-balancing-trees-why-std-map-is-fast"
      },
      {
        "level": 2,
        "txt": "Trees in disguise",
        "id": "trees-in-disguise"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "tries",
    "title": "Tries — prefix trees for strings",
    "html": "<blockquote class=\"lec-callout\">A <strong>trie</strong> (from re<em>trie</em>val, usually said \"try\") stores a set of strings by <strong>sharing common prefixes</strong>. Each node is one character; the path from the root spells a prefix. Insert and lookup cost <strong>O(L)</strong> in the key length — independent of how many words are stored — and it answers <strong>prefix</strong> queries a hash set simply can't.</blockquote>\n<hr>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-trie.svg\" alt=\"Trie storing cat/car/can/dog with shared prefixes and end-of-word markers\" loading=\"lazy\"></p>\n<h2 id=\"structure\">Structure</h2>\n<p>Each node holds:</p>\n<ul><li>a map/array of <strong>children</strong> keyed by the next character, and</li><li>an **<code>is_end</code>** flag marking that a complete word ends here.</li></ul>\n<p>Words that share a prefix share the path for that prefix — \"cat\", \"car\", \"can\" all reuse the <code>c → a</code> edges, then branch. That sharing is the whole point: memory scales with <em>distinct prefixes</em>, and any prefix query walks one edge per character.</p>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Cost</th><th>Note</th></tr></thead><tbody><tr><td>insert(word)</td><td>O(L)</td><td>create missing child nodes down the path</td></tr><tr><td>search(word)</td><td>O(L)</td><td>walk the path; check <code>is_end</code> at the end</td></tr><tr><td>startsWith(prefix)</td><td>O(L)</td><td>walk the path; existence is enough</td></tr><tr><td>space</td><td>O(total chars)</td><td>worst case; shared prefixes save memory</td></tr></tbody></table></div>\n<p><code>L</code> = length of the word/prefix. Note there's <strong>no dependence on N</strong> (the number of stored words) — that's the trie's edge over scanning a list or even hashing when prefixes matter.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>insert(word):\n    node = root\n    for ch in word:\n        if ch not in node.children:\n            node.children[ch] = new Node()\n        node = node.children[ch]\n    node.is_end = True\n\nsearch(word):                       # exact word present?\n    node = walk(word)\n    return node is not None and node.is_end\n\nstartsWith(prefix):                 # any word with this prefix?\n    return walk(prefix) is not None</code></pre>\n<hr>\n<h2 id=\"implementation-choices\">Implementation choices</h2>\n<ul><li><strong>Children as a hash map</strong> (<code>dict[char] → node</code>): flexible, any alphabet (Unicode, mixed case).</li><li><strong>Children as a fixed array</strong> (<code>array&lt;Node*, 26&gt;</code> for <code>a–z</code>): faster and cache-friendlier, but wastes slots on sparse alphabets. Pick based on the character set.</li></ul>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th></th><th>Python</th><th>C++</th></tr></thead><tbody><tr><td>children</td><td><code>dict</code> (or <code>defaultdict</code>)</td><td><code>array&lt;Trie*,26&gt;</code> or <code>unordered_map&lt;char,Trie*&gt;</code></td></tr><tr><td>end flag</td><td><code>is_end: bool</code></td><td><code>bool isEnd</code></td></tr></tbody></table></div>\n<div class=\"sol\"><div class=\"sol-title\">Implement Trie — insert / search / startsWith</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class Trie:\n    def __init__(self):\n        self.children = {}          # char -&gt; Trie\n        self.is_end = False\n\n    def insert(self, word):\n        node = self\n        for c in word:\n            node = node.children.setdefault(c, Trie())\n        node.is_end = True\n\n    def _walk(self, s):\n        node = self\n        for c in s:\n            if c not in node.children:\n                return None\n            node = node.children[c]\n        return node\n\n    def search(self, word):\n        node = self._walk(word)\n        return node is not None and node.is_end\n\n    def starts_with(self, prefix):\n        return self._walk(prefix) is not None</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>struct Trie {\n    array&lt;Trie*, 26&gt; child{};       // 'a'..'z', all null\n    bool isEnd = false;\n\n    void insert(const string&amp; w) {\n        Trie* node = this;\n        for (char c : w) {\n            int i = c - 'a';\n            if (!node-&gt;child[i]) node-&gt;child[i] = new Trie();\n            node = node-&gt;child[i];\n        }\n        node-&gt;isEnd = true;\n    }\n    Trie* walk(const string&amp; s) {\n        Trie* node = this;\n        for (char c : s) {\n            int i = c - 'a';\n            if (!node-&gt;child[i]) return nullptr;\n            node = node-&gt;child[i];\n        }\n        return node;\n    }\n    bool search(const string&amp; w)    { Trie* n = walk(w); return n &amp;&amp; n-&gt;isEnd; }\n    bool startsWith(const string&amp; p){ return walk(p) != nullptr; }\n};</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Wildcard search — <code>.</code> matches any character (branch the walk)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class WordDictionary:\n    def __init__(self):\n        self.root = {}              # nested dicts; \"$\" marks end of word\n\n    def add(self, word):\n        node = self.root\n        for c in word:\n            node = node.setdefault(c, {})\n        node[\"$\"] = True\n\n    def search(self, word):\n        def dfs(node, i):\n            if i == len(word):\n                return \"$\" in node\n            c = word[i]\n            if c == \".\":                        # try every child\n                return any(dfs(child, i + 1)\n                           for k, child in node.items() if k != \"$\")\n            return c in node and dfs(node[c], i + 1)\n        return dfs(self.root, 0)</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>struct WordDictionary {\n    struct Node { unordered_map&lt;char, Node*&gt; ch; bool end = false; };\n    Node* root = new Node();\n\n    void add(const string&amp; w) {\n        Node* n = root;\n        for (char c : w) { if (!n-&gt;ch.count(c)) n-&gt;ch[c] = new Node(); n = n-&gt;ch[c]; }\n        n-&gt;end = true;\n    }\n    bool dfs(Node* n, const string&amp; w, int i) {\n        if (i == (int)w.size()) return n-&gt;end;\n        char c = w[i];\n        if (c == '.') {                         // any child\n            for (auto&amp; [k, nxt] : n-&gt;ch)\n                if (dfs(nxt, w, i + 1)) return true;\n            return false;\n        }\n        return n-&gt;ch.count(c) &amp;&amp; dfs(n-&gt;ch[c], w, i + 1);\n    }\n    bool search(const string&amp; w) { return dfs(root, w, 0); }\n};</code></pre></div></div>\n<hr>\n<h2 id=\"where-tries-shine\">Where tries shine</h2>\n<ul><li><strong>Autocomplete / typeahead</strong> — walk to the prefix node, then DFS to enumerate completions.</li><li><strong>Spell-check / dictionary</strong> — membership plus near-miss suggestions.</li><li><strong>Wildcard / regex-lite matching</strong> — branch on <code>.</code> (above).</li><li><strong>Word-search / Boggle boards</strong> — one trie of the dictionary prunes DFS across the grid.</li><li><strong>Longest-prefix matching</strong> — IP routing tables, phone prefixes.</li></ul>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>\"Prefix\", \"startsWith\", \"autocomplete\", \"dictionary of words\"</strong> → trie.</li><li>**Wildcard <code>.</code> matching** → DFS the trie, branching on <code>.</code>.</li><li><strong>Many words searched on a grid</strong> → build one trie, DFS the grid against it (prune dead paths).</li><li>State <strong>O(L)</strong>, independent of the number of stored words.</li><li>Choose <strong>array vs hash-map children</strong> by alphabet size.</li></ol>\n<details class=\"qa\"><summary>A hash set already gives O(1) word lookup — why ever use a trie?</summary><div class=\"qa-body\"><p>A hash set answers \"<strong>is this exact word present?</strong>\" but cannot answer \"**does any word start with <code>pre</code>?<strong>\" without scanning every key. A trie answers </strong>prefix** queries in O(L), enumerates all completions of a prefix, supports wildcard matching, and shares memory across common prefixes. Use a hash set for exact membership; a trie whenever prefixes matter.</p></div></details>\n<details class=\"qa\"><summary>How does a trie power autocomplete?</summary><div class=\"qa-body\"><p>Walk from the root to the node at the end of the typed prefix — O(L). Everything in that node's subtree is a valid completion, so <strong>DFS the subtree</strong> collecting words that have <code>is_end</code> set. Rank by frequency (store a count per word) or cache the top-k completions at each node for instant suggestions.</p></div></details>\n<details class=\"qa\"><summary>What's the memory cost of a trie, and how do you cut it?</summary><div class=\"qa-body\"><p>Worst case is O(total characters across all words), and a hash-map-per-node has pointer overhead. A <strong>radix tree (compressed trie)</strong> merges chains of single-child nodes into one edge labeled with a substring, collapsing long non-branching paths. Using a fixed child <strong>array</strong> instead of a hash map also trims per-node overhead for a small alphabet.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "Structure",
        "id": "structure"
      },
      {
        "level": 2,
        "txt": "Implementation choices",
        "id": "implementation-choices"
      },
      {
        "level": 2,
        "txt": "Where tries shine",
        "id": "where-tries-shine"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "graphs",
    "title": "Graphs — nodes and the edges between them",
    "html": "<blockquote class=\"lec-callout\">A <strong>graph</strong> is a set of <strong>nodes (vertices)</strong> connected by <strong>edges</strong> — the most general relational structure (a tree is just a graph with no cycles and one root). This page is about <strong>representing</strong> a graph and the two traversals — <strong>BFS</strong> and <strong>DFS</strong> — that everything else is built on.</blockquote>\n<hr>\n<p><img class=\"lec-img\" src=\"study-materials/images/ds-graph.svg\" alt=\"Graph stored as an adjacency list and as an adjacency matrix\" loading=\"lazy\"></p>\n<h2 id=\"flavors\">Flavors</h2>\n<ul><li><strong>Undirected vs directed</strong> — is an edge two-way (<code>friends</code>) or one-way (<code>follows</code>, <code>depends-on</code>)?</li><li><strong>Weighted vs unweighted</strong> — does each edge carry a cost (distance, time)?</li><li><strong>Cyclic vs acyclic</strong> — a directed acyclic graph (<strong>DAG</strong>) can be <strong>topologically sorted</strong> (schedules, build systems, course prerequisites).</li><li><strong>Connected / components</strong> — is every node reachable, or are there islands?</li></ul>\n<h2 id=\"two-representations\">Two representations</h2>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th></th><th>Adjacency list</th><th>Adjacency matrix</th></tr></thead><tbody><tr><td>Space</td><td><strong>O(V + E)</strong></td><td>O(V²)</td></tr><tr><td>\"Is u–v an edge?\"</td><td>O(deg u)</td><td><strong>O(1)</strong></td></tr><tr><td>Iterate neighbors of u</td><td>O(deg u)</td><td>O(V)</td></tr><tr><td>Best for</td><td><strong>sparse</strong> graphs (the default)</td><td>dense graphs / constant-time edge tests</td></tr></tbody></table></div>\n<p>Almost always use an <strong>adjacency list</strong> — a <code>dict</code>/array mapping each node to its neighbors.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code># adjacency list\ngraph = defaultdict(list)\nfor (u, v) in edges:\n    graph[u].append(v)\n    graph[v].append(u)        # omit this line for a directed graph</code></pre>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th></th><th>Python</th><th>C++</th></tr></thead><tbody><tr><td>adjacency list</td><td><code>defaultdict(list)</code></td><td><code>vector&lt;vector&lt;int&gt;&gt;</code></td></tr><tr><td>visited set</td><td><code>set()</code></td><td><code>vector&lt;bool&gt;</code></td></tr><tr><td>BFS queue</td><td><code>collections.deque</code></td><td><code>queue&lt;int&gt;</code></td></tr></tbody></table></div>\n<hr>\n<h2 id=\"bfs-breadth-first-a-queue\">BFS — breadth-first (a queue)</h2>\n<p>Explore in rings of increasing distance from the source. On an <strong>unweighted</strong> graph BFS finds the <strong>shortest path</strong> (fewest edges), because it reaches every node in non-decreasing hop order. O(V+E).</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>bfs(start):\n    q = deque([start]); visited = {start}\n    dist = {start: 0}\n    while q:\n        u = q.popleft()\n        for v in graph[u]:\n            if v not in visited:\n                visited.add(v)\n                dist[v] = dist[u] + 1       # first time seen = shortest\n                q.append(v)</code></pre>\n<h2 id=\"dfs-depth-first-a-stack-or-recursion\">DFS — depth-first (a stack or recursion)</h2>\n<p>Dive as deep as possible, then backtrack. The tool for <strong>reachability, connected components, cycle detection, topological sort, and enumerating paths</strong>. O(V+E).</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>dfs(u):\n    visited.add(u)\n    for v in graph[u]:\n        if v not in visited:\n            dfs(v)</code></pre>\n<blockquote class=\"lec-callout\"><strong>Pick by the question.</strong> <em>Shortest path / fewest steps / level-by-level</em> → BFS. <em>Reachability / components / cycles / all paths / ordering</em> → DFS. Weighted shortest path → <strong>Dijkstra</strong> (a BFS with a min-heap frontier).</blockquote>\n<div class=\"sol\"><div class=\"sol-title\">Number of connected components — DFS flood fill</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def count_components(n, edges):\n    graph = [[] for _ in range(n)]\n    for u, v in edges:\n        graph[u].append(v)\n        graph[v].append(u)\n\n    seen = [False] * n\n    def dfs(u):\n        seen[u] = True\n        for v in graph[u]:\n            if not seen[v]:\n                dfs(v)\n\n    count = 0\n    for i in range(n):\n        if not seen[i]:                 # a new island\n            count += 1\n            dfs(i)\n    return count</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>int countComponents(int n, vector&lt;vector&lt;int&gt;&gt;&amp; edges) {\n    vector&lt;vector&lt;int&gt;&gt; g(n);\n    for (auto&amp; e : edges) { g[e[0]].push_back(e[1]); g[e[1]].push_back(e[0]); }\n    vector&lt;bool&gt; seen(n, false);\n    function&lt;void(int)&gt; dfs = [&amp;](int u) {\n        seen[u] = true;\n        for (int v : g[u]) if (!seen[v]) dfs(v);\n    };\n    int count = 0;\n    for (int i = 0; i &lt; n; ++i)\n        if (!seen[i]) { ++count; dfs(i); }      // new island\n    return count;\n}</code></pre></div></div>\n<div class=\"sol\"><div class=\"sol-title\">Course Schedule — cycle detection via topological sort (Kahn's BFS)</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>from collections import deque\n\ndef can_finish(num, prereqs):\n    graph = [[] for _ in range(num)]\n    indeg = [0] * num\n    for course, need in prereqs:            # need -&gt; course\n        graph[need].append(course)\n        indeg[course] += 1\n\n    q = deque(i for i in range(num) if indeg[i] == 0)\n    done = 0\n    while q:\n        u = q.popleft()\n        done += 1\n        for v in graph[u]:\n            indeg[v] -= 1                    # remove edge u-&gt;v\n            if indeg[v] == 0:\n                q.append(v)\n    return done == num                      # all placed =&gt; no cycle</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>bool canFinish(int num, vector&lt;vector&lt;int&gt;&gt;&amp; prereqs) {\n    vector&lt;vector&lt;int&gt;&gt; g(num);\n    vector&lt;int&gt; indeg(num, 0);\n    for (auto&amp; p : prereqs) { g[p[1]].push_back(p[0]); indeg[p[0]]++; }\n    queue&lt;int&gt; q;\n    for (int i = 0; i &lt; num; ++i) if (indeg[i] == 0) q.push(i);\n    int done = 0;\n    while (!q.empty()) {\n        int u = q.front(); q.pop(); ++done;\n        for (int v : g[u]) if (--indeg[v] == 0) q.push(v);\n    }\n    return done == num;                     // no cycle\n}</code></pre></div></div>\n<hr>\n<h2 id=\"the-neighbor-functions-you-ll-actually-build\">The neighbor functions you'll actually build</h2>\n<p>Many \"graph\" problems have an <strong>implicit</strong> graph — a grid, a set of word transformations, a state space. There's no adjacency list; you generate neighbors on the fly.</p>\n<pre class=\"lec-pre\" data-lang=\"text\"><code># grid: 4-directional neighbors\nfor dr, dc in [(-1,0),(1,0),(0,-1),(0,1)]:\n    nr, nc = r + dr, c + dc\n    if 0 &lt;= nr &lt; rows and 0 &lt;= nc &lt; cols and grid[nr][nc] == '1':\n        ...   # BFS/DFS from here</code></pre>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li>Build an <strong>adjacency list</strong> unless the graph is dense or you need O(1) edge tests.</li><li><strong>Shortest path, unweighted</strong> → BFS; <strong>weighted</strong> → Dijkstra (heap); <strong>components/cycles/order</strong> → DFS or Kahn's algorithm.</li><li><strong>Grid / word ladder / state space</strong> → implicit graph; write a <code>neighbors()</code> generator.</li><li>Always carry a **<code>visited</code>** set — cycles cause infinite loops otherwise.</li><li>State <strong>O(V + E)</strong> for a plain traversal.</li></ol>\n<details class=\"qa\"><summary>BFS vs DFS — how do you choose in one sentence?</summary><div class=\"qa-body\"><p>Use <strong>BFS</strong> when distance/level matters — shortest path on an unweighted graph, \"minimum number of steps,\" or level-by-level output — because it visits nodes in non-decreasing hop order. Use <strong>DFS</strong> when structure matters — reachability, connected components, cycle detection, topological ordering, or enumerating all paths — because it naturally explores and backtracks.</p></div></details>\n<details class=\"qa\"><summary>Why does plain BFS give the shortest path only on <em>unweighted</em> graphs, and what replaces it?</summary><div class=\"qa-body\"><p>BFS expands nodes in order of hop count, so the first time it reaches a node is via the fewest edges — optimal only when every edge costs the same. With <strong>weights</strong>, fewest edges ≠ least cost, so you switch to <strong>Dijkstra</strong> (a priority-queue frontier that always expands the lowest-cost node), or Bellman-Ford when weights can be negative.</p></div></details>\n<details class=\"qa\"><summary>How do you detect a cycle — and does directed vs undirected change the method?</summary><div class=\"qa-body\"><p><strong>Directed:</strong> either DFS with three colors (white/gray/black) — a back-edge to a <em>gray</em> (in-progress) node is a cycle — or <strong>Kahn's topological sort</strong>: if you can't place all nodes (some keep nonzero in-degree), there's a cycle. <strong>Undirected:</strong> DFS and treat any visited neighbor that isn't your parent as a cycle, or use <strong>union-find</strong> — an edge whose endpoints are already in the same set closes a cycle.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "Flavors",
        "id": "flavors"
      },
      {
        "level": 2,
        "txt": "Two representations",
        "id": "two-representations"
      },
      {
        "level": 2,
        "txt": "BFS — breadth-first (a queue)",
        "id": "bfs-breadth-first-a-queue"
      },
      {
        "level": 2,
        "txt": "DFS — depth-first (a stack or recursion)",
        "id": "dfs-depth-first-a-stack-or-recursion"
      },
      {
        "level": 2,
        "txt": "The neighbor functions you'll actually build",
        "id": "the-neighbor-functions-you-ll-actually-build"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  },
  {
    "slug": "union-find",
    "title": "Union-Find (DSU) — dynamic connectivity in ~O(1)",
    "html": "<blockquote class=\"lec-callout\"><strong>Union-Find</strong>, a.k.a. the <strong>disjoint-set union (DSU)</strong>, tracks a partition of elements into groups and answers <em>\"are these two in the same group?\"</em> while groups keep merging. Two operations — <strong>find</strong> and <strong>union</strong> — both run in <strong>near-constant</strong> amortized time. It's the tidiest tool for <strong>dynamic connectivity</strong> and <strong>cycle detection while adding edges</strong>.</blockquote>\n<hr>\n<p><img class=\"lec-img\" src=\"study-materials/images/lc-union-find.svg\" alt=\"Union-find: parent-pointer trees merged by union and flattened by path compression\" loading=\"lazy\"></p>\n<h2 id=\"the-idea\">The idea</h2>\n<p>Each group is a tree of <strong>parent pointers</strong>; the <strong>root</strong> is the group's representative.</p>\n<ul><li><strong>find(x)</strong> — follow parents up to the root. Two elements are in the same group iff they share a root.</li><li><strong>union(a, b)</strong> — link one root under the other, merging two trees into one.</li></ul>\n<p>Naively these trees can grow tall (O(n) find). Two optimizations flatten them so both operations become <strong>α(n)</strong> amortized — the inverse Ackermann function, ≤ 4 for any input you'll ever see, i.e. effectively constant:</p>\n<ul><li><strong>Path compression</strong> — during <code>find</code>, repoint nodes directly at the root, flattening the path.</li><li><strong>Union by rank/size</strong> — always attach the <em>shorter/smaller</em> tree under the taller/larger one, so height barely grows.</li></ul>\n<pre class=\"lec-pre\" data-lang=\"text\"><code>find(x):\n    while parent[x] != x:\n        parent[x] = parent[parent[x]]     # path compression (halving)\n        x = parent[x]\n    return x\n\nunion(a, b):\n    ra, rb = find(a), find(b)\n    if ra == rb: return False             # already together (a cycle, if an edge)\n    if rank[ra] &lt; rank[rb]: swap(ra, rb)  # attach smaller under larger\n    parent[rb] = ra\n    if rank[ra] == rank[rb]: rank[ra] += 1\n    return True</code></pre>\n<div class=\"lec-table-wrap\"><table class=\"lec-table\"><thead><tr><th>Operation</th><th>Cost (amortized)</th></tr></thead><tbody><tr><td>find</td><td>~O(α(n)) ≈ O(1)</td></tr><tr><td>union</td><td>~O(α(n)) ≈ O(1)</td></tr><tr><td>build (n singletons)</td><td>O(n)</td></tr></tbody></table></div>\n<hr>\n<h2 id=\"reference-implementation\">Reference implementation</h2>\n<div class=\"sol\"><div class=\"sol-title\">Union-Find with path compression + union by rank</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>class DSU:\n    def __init__(self, n):\n        self.parent = list(range(n))     # each element is its own root\n        self.rank = [0] * n\n        self.count = n                   # number of disjoint groups\n\n    def find(self, x):\n        while self.parent[x] != x:\n            self.parent[x] = self.parent[self.parent[x]]   # path compression\n            x = self.parent[x]\n        return x\n\n    def union(self, a, b):\n        ra, rb = self.find(a), self.find(b)\n        if ra == rb:\n            return False                 # already connected\n        if self.rank[ra] &lt; self.rank[rb]:\n            ra, rb = rb, ra\n        self.parent[rb] = ra\n        if self.rank[ra] == self.rank[rb]:\n            self.rank[ra] += 1\n        self.count -= 1                  # two groups merged into one\n        return True\n\n    def connected(self, a, b):\n        return self.find(a) == self.find(b)</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>struct DSU {\n    vector&lt;int&gt; parent, rank_;\n    int count;\n    DSU(int n) : parent(n), rank_(n, 0), count(n) {\n        iota(parent.begin(), parent.end(), 0);       // parent[i] = i\n    }\n    int find(int x) {\n        while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n        return x;                                     // path compression\n    }\n    bool unite(int a, int b) {\n        int ra = find(a), rb = find(b);\n        if (ra == rb) return false;                   // already connected\n        if (rank_[ra] &lt; rank_[rb]) swap(ra, rb);\n        parent[rb] = ra;\n        if (rank_[ra] == rank_[rb]) rank_[ra]++;\n        --count;\n        return true;\n    }\n    bool connected(int a, int b) { return find(a) == find(b); }\n};</code></pre></div></div>\n<hr>\n<h2 id=\"what-it-s-for\">What it's for</h2>\n<ul><li><strong>Dynamic connectivity</strong> — edges arrive over time; repeatedly ask \"are a and b connected yet?\"</li><li><strong>Cycle detection (undirected)</strong> — a <code>union</code> whose endpoints already share a root closes a cycle.</li><li><strong>Kruskal's MST</strong> — add edges cheapest-first, skipping any that would form a cycle.</li><li><strong>Counting components / accounts merge / friend circles</strong> — merge, then count distinct roots.</li><li><strong>Grid percolation / islands with a growing set of open cells.</strong></li></ul>\n<div class=\"sol\"><div class=\"sol-title\">Redundant Connection — the edge that creates a cycle</div><div class=\"sol-tabs\" role=\"tablist\"><button class=\"sol-tab active\" data-lang=\"python\" type=\"button\">Python</button><button class=\"sol-tab\" data-lang=\"cpp\" type=\"button\">C++</button></div><div class=\"sol-pane\" data-lang=\"python\"><pre class=\"lec-pre\" data-lang=\"python\"><code>def find_redundant(edges):\n    dsu = DSU(len(edges) + 1)           # nodes are 1..n\n    for u, v in edges:\n        if not dsu.union(u, v):         # u and v already connected\n            return [u, v]               # this edge closes the cycle\n    return []</code></pre></div><div class=\"sol-pane\" data-lang=\"cpp\" hidden><pre class=\"lec-pre\" data-lang=\"cpp\"><code>vector&lt;int&gt; findRedundant(vector&lt;vector&lt;int&gt;&gt;&amp; edges) {\n    DSU dsu(edges.size() + 1);          // nodes 1..n\n    for (auto&amp; e : edges)\n        if (!dsu.unite(e[0], e[1]))     // already connected\n            return {e[0], e[1]};\n    return {};\n}</code></pre></div></div>\n<hr>\n<h2 id=\"interview-checklist\">Interview checklist</h2>\n<ol><li><strong>\"Dynamic connectivity\" / \"are these connected?\" as edges are added</strong> → union-find.</li><li><strong>Undirected cycle detection / MST (Kruskal)</strong> → union whose endpoints already share a root.</li><li><strong>Count groups</strong> → track a <code>count</code>, decrement on each successful union.</li><li>Always apply <strong>both</strong> optimizations (path compression + union by rank) to claim ~O(1).</li><li>Map arbitrary labels (strings, coordinates) to integer indices first.</li></ol>\n<details class=\"qa\"><summary>Union-find vs BFS/DFS for connectivity — when does each win?</summary><div class=\"qa-body\"><p>Use <strong>BFS/DFS</strong> when the graph is fixed and you want to explore it once (list components, find paths, compute distances). Use <strong>union-find</strong> when connectivity is <strong>incremental</strong> — edges keep arriving and you repeatedly ask \"connected yet?\" — because it answers each query in ~O(1) without re-traversing. It also detects cycles online as edges are added. Union-find gives <em>connectivity</em>, not <em>paths</em> — it can't tell you the route between two nodes.</p></div></details>\n<details class=\"qa\"><summary>What do path compression and union by rank each contribute?</summary><div class=\"qa-body\"><p><strong>Union by rank/size</strong> keeps trees shallow by always hanging the smaller tree under the larger, so height grows logarithmically at worst. <strong>Path compression</strong> flattens the path to the root on every <code>find</code>, so repeated queries get cheaper over time. Individually each gives ~O(log n); <strong>together</strong> they give the near-constant α(n) amortized bound. Skipping one leaves you with logarithmic finds.</p></div></details>\n<details class=\"qa\"><summary>Can you undo a union or delete an edge from a DSU?</summary><div class=\"qa-body\"><p>Not with path compression — it destroys the structure needed to roll back. For \"offline\" problems that require deletions, use <strong>union-find with rollback</strong> (union by rank only, no compression, keep a stack of changes) or process queries in reverse and turn deletions into unions. Plain DSU is <strong>incremental-merge only</strong>.</p></div></details>",
    "toc": [
      {
        "level": 2,
        "txt": "The idea",
        "id": "the-idea"
      },
      {
        "level": 2,
        "txt": "Reference implementation",
        "id": "reference-implementation"
      },
      {
        "level": 2,
        "txt": "What it's for",
        "id": "what-it-s-for"
      },
      {
        "level": 2,
        "txt": "Interview checklist",
        "id": "interview-checklist"
      }
    ]
  }
];
